import { a as createMiddleware } from "./server-COznR7QB.js";
import { g as getSessionClaims } from "./db-D5OYORU-.js";
const requireAuth = createMiddleware({ type: "function" }).server(async ({ next }) => {
  const claims = await getSessionClaims();
  if (!claims) throw new Error("Unauthorized");
  return next({
    context: {
      userId: claims.sub,
      email: claims.email,
      roles: claims.roles,
      claims
    }
  });
});
const requireAdmin = createMiddleware({ type: "function" }).middleware([requireAuth]).server(async ({ next, context }) => {
  if (!context.roles?.includes("admin")) throw new Error("Forbidden");
  return next();
});
export {
  requireAuth as a,
  requireAdmin as r
};
