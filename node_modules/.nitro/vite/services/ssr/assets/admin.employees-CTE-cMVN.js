import { jsx, jsxs } from "react/jsx-runtime";
import { u as useServerFn, V as listEmployees, W as createEmployee, X as deleteEmployee, Y as listRoles, v as getRoleLabel } from "./router-CtQuP2fc.js";
import { useQueryClient, useQuery, useMutation } from "@tanstack/react-query";
import { useState, useEffect } from "react";
import { Loader2, Plus, Trash2, X } from "lucide-react";
import { toast } from "sonner";
import "@tanstack/react-router";
import "./createSsrRpc-DY9HpWEz.js";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "./auth-middleware.server-B9hAjfqi.js";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
import "zod";
import "./vip.repo-CycBrLVA.js";
import "./saudi-cities-D2sGDQV3.js";
import "@aws-sdk/client-s3";
import "./r2-CJ2zxhhj.js";
import "aws4fetch";
import "@lovable.dev/webhooks-js";
import "@react-email/components";
import "@lovable.dev/email-js";
function EmployeesPage() {
  const list = useServerFn(listEmployees);
  const create = useServerFn(createEmployee);
  const del = useServerFn(deleteEmployee);
  const rolesFn = useServerFn(listRoles);
  const qc = useQueryClient();
  const {
    data,
    isLoading,
    error
  } = useQuery({
    queryKey: ["employees"],
    queryFn: () => list()
  });
  const {
    data: roles
  } = useQuery({
    queryKey: ["roles"],
    queryFn: () => rolesFn()
  });
  const [open, setOpen] = useState(false);
  const createMut = useMutation({
    mutationFn: (v) => create({
      data: v
    }),
    onSuccess: () => {
      toast.success("تم الإنشاء");
      qc.invalidateQueries({
        queryKey: ["employees"]
      });
      setOpen(false);
    },
    onError: (e) => toast.error(e.message)
  });
  const delMut = useMutation({
    mutationFn: (user_id) => del({
      data: {
        user_id
      }
    }),
    onSuccess: () => {
      toast.success("تم الحذف");
      qc.invalidateQueries({
        queryKey: ["employees"]
      });
    },
    onError: (e) => toast.error(e.message)
  });
  if (isLoading) return /* @__PURE__ */ jsx("div", { className: "grid place-items-center py-20", children: /* @__PURE__ */ jsx(Loader2, { className: "h-6 w-6 animate-spin" }) });
  if (error) return /* @__PURE__ */ jsx("p", { className: "text-destructive", children: error.message });
  return /* @__PURE__ */ jsxs("div", { children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-6 flex items-center justify-between", children: [
      /* @__PURE__ */ jsxs("h1", { className: "text-2xl font-bold", children: [
        "المستخدمون (",
        data?.length ?? 0,
        ")"
      ] }),
      /* @__PURE__ */ jsxs("button", { onClick: () => setOpen(true), className: "inline-flex items-center gap-1.5 rounded-md bg-foreground px-3 py-2 text-sm font-semibold text-background hover:bg-foreground/90", children: [
        /* @__PURE__ */ jsx(Plus, { className: "h-4 w-4" }),
        " إضافة مستخدم"
      ] })
    ] }),
    /* @__PURE__ */ jsx("div", { className: "overflow-hidden rounded-xl border border-border bg-card", children: /* @__PURE__ */ jsxs("table", { className: "w-full text-sm", children: [
      /* @__PURE__ */ jsx("thead", { className: "bg-secondary/60 text-right", children: /* @__PURE__ */ jsxs("tr", { children: [
        /* @__PURE__ */ jsx("th", { className: "p-3", children: "البريد" }),
        /* @__PURE__ */ jsx("th", { className: "p-3", children: "الدور" }),
        /* @__PURE__ */ jsx("th", { className: "p-3", children: "تاريخ الإنشاء" }),
        /* @__PURE__ */ jsx("th", { className: "p-3" })
      ] }) }),
      /* @__PURE__ */ jsx("tbody", { children: (data ?? []).map((u) => /* @__PURE__ */ jsxs("tr", { className: "border-t border-border", children: [
        /* @__PURE__ */ jsx("td", { className: "p-3 font-medium", children: u.email }),
        /* @__PURE__ */ jsx("td", { className: "p-3", children: /* @__PURE__ */ jsx("span", { className: "rounded-full bg-secondary px-2 py-0.5 text-xs", children: getRoleLabel(u.role) }) }),
        /* @__PURE__ */ jsx("td", { className: "p-3 text-muted-foreground", children: new Date(u.created_at).toLocaleDateString("ar") }),
        /* @__PURE__ */ jsx("td", { className: "p-3 text-left", children: /* @__PURE__ */ jsxs("button", { onClick: () => {
          if (confirm(`حذف ${u.email}؟`)) delMut.mutate(u.user_id);
        }, className: "inline-flex items-center gap-1 rounded-md border border-destructive/30 px-2 py-1 text-xs text-destructive hover:bg-destructive/10", children: [
          /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" }),
          " حذف"
        ] }) })
      ] }, u.user_id + u.role)) })
    ] }) }),
    open ? /* @__PURE__ */ jsx(NewEmployeeModal, { roles: roles ?? [], onClose: () => setOpen(false), onSave: (v) => createMut.mutate(v), saving: createMut.isPending }) : null
  ] });
}
function NewEmployeeModal({
  onClose,
  onSave,
  saving,
  roles
}) {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [roleId, setRoleId] = useState("");
  useEffect(() => {
    if (!roleId && roles.length > 0) {
      const employeeRole = roles.find((r) => r.name === "employee") ?? roles[0];
      setRoleId(employeeRole.id);
    }
  }, [roles, roleId]);
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-50 grid place-items-center bg-black/50 p-4", onClick: onClose, children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-md rounded-2xl bg-card p-6 shadow-xl", onClick: (e) => e.stopPropagation(), children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-4 flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-xl font-bold", children: "مستخدم جديد" }),
      /* @__PURE__ */ jsx("button", { onClick: onClose, children: /* @__PURE__ */ jsx(X, { className: "h-5 w-5" }) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-3", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("label", { className: "mb-1.5 block text-sm font-semibold", children: "البريد" }),
        /* @__PURE__ */ jsx("input", { type: "email", className: "inp", value: email, onChange: (e) => setEmail(e.target.value) })
      ] }),
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("label", { className: "mb-1.5 block text-sm font-semibold", children: "كلمة المرور" }),
        /* @__PURE__ */ jsx("input", { type: "password", autoComplete: "new-password", className: "inp", value: password, onChange: (e) => setPassword(e.target.value) })
      ] }),
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("label", { className: "mb-1.5 block text-sm font-semibold", children: "الدور" }),
        /* @__PURE__ */ jsx("select", { className: "inp", value: roleId, onChange: (e) => setRoleId(e.target.value), children: roles.map((r) => /* @__PURE__ */ jsx("option", { value: r.id, children: r.label }, r.id)) })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "mt-5 flex gap-2", children: [
      /* @__PURE__ */ jsxs("button", { disabled: saving || !email || password.length < 6 || !roleId, onClick: () => onSave({
        email,
        password,
        role_id: roleId
      }), className: "flex-1 inline-flex items-center justify-center gap-2 rounded-md bg-foreground px-4 py-2.5 text-sm font-semibold text-background hover:bg-foreground/90 disabled:opacity-60", children: [
        saving ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }) : null,
        " إنشاء"
      ] }),
      /* @__PURE__ */ jsx("button", { onClick: onClose, className: "rounded-md border border-border px-4 py-2.5 text-sm", children: "إلغاء" })
    ] })
  ] }) });
}
export {
  EmployeesPage as component
};
