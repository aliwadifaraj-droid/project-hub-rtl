import { d as db, r as rowsToObjects } from "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "react";
import "@tanstack/react-router";
import "react/jsx-runtime";
import "@tanstack/react-router/ssr/server";
import "@libsql/client";
function decode(row) {
  const receipt = row.receipt_path ?? row.receipt_key ?? null;
  return {
    id: String(row.id),
    name: row.name ?? null,
    email: row.email ?? null,
    plan: row.plan ?? null,
    city: row.city ?? null,
    status: String(row.status ?? "pending"),
    project_id: row.project_id ?? null,
    expires_at: row.expires_at ?? null,
    receipt_path: receipt,
    receipt_key: receipt,
    notes: row.notes ?? null,
    ocr_bank: row.ocr_bank ?? null,
    ocr_amount: row.ocr_amount ?? null,
    ocr_date: row.ocr_date ?? null,
    ocr_time: row.ocr_time ?? null,
    created_at: String(row.created_at ?? "")
  };
}
let _colsReady = null;
function ensureColumns() {
  if (_colsReady) return _colsReady;
  _colsReady = Promise.all([
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN city TEXT`).catch(() => void 0),
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN project_id TEXT`).catch(() => void 0),
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN expires_at TEXT`).catch(() => void 0),
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN ocr_bank TEXT`).catch(() => void 0),
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN ocr_amount TEXT`).catch(() => void 0),
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN ocr_date TEXT`).catch(() => void 0),
    db.execute(`ALTER TABLE vip_subscribers ADD COLUMN ocr_time TEXT`).catch(() => void 0),
    db.execute(`UPDATE vip_subscribers SET status = 'expired' WHERE status = 'rejected' AND expires_at IS NOT NULL AND datetime(expires_at) <= datetime('now')`).catch(() => void 0)
  ]).then(() => void 0);
  return _colsReady;
}
function ensureCityColumn() {
  return ensureColumns();
}
async function listVipSubscribers() {
  await ensureColumns();
  const r = await db.execute(`SELECT * FROM vip_subscribers ORDER BY created_at DESC`);
  return rowsToObjects(r).map(decode);
}
async function listVipWithProjectNames() {
  await ensureColumns();
  const r = await db.execute(
    `SELECT v.*, p.name AS project_name
     FROM vip_subscribers v
     LEFT JOIN projects p ON p.id = v.project_id
     ORDER BY v.created_at DESC`
  );
  return rowsToObjects(r).map((row) => ({
    ...decode(row),
    project_name: row.project_name ?? null
  }));
}
async function approveByProject(projectId) {
  await ensureColumns();
  const expiresAt = new Date(Date.now() + 30 * 24 * 36e5).toISOString();
  await db.execute(
    `UPDATE vip_subscribers SET status = 'approved', expires_at = ? WHERE project_id = ?`,
    [expiresAt, projectId]
  );
  const r = await db.execute(
    `SELECT * FROM vip_subscribers WHERE project_id = ? ORDER BY created_at DESC LIMIT 1`,
    [projectId]
  );
  const row = rowsToObjects(r)[0];
  return row ? decode(row) : null;
}
async function cancelByProject(projectId) {
  await ensureColumns();
  await db.execute(
    `UPDATE vip_subscribers SET status = 'rejected', expires_at = NULL WHERE project_id = ?`,
    [projectId]
  );
}
async function listAllApprovedWithProject() {
  await ensureColumns();
  const r = await db.execute(
    `SELECT project_id, expires_at FROM vip_subscribers
      WHERE status = 'approved' AND project_id IS NOT NULL
      ORDER BY created_at DESC`
  );
  return rowsToObjects(r).map((row) => ({
    project_id: String(row.project_id),
    expires_at: row.expires_at ?? null
  }));
}
async function stopVipByCity(city) {
  await ensureCityColumn();
  const r = await db.execute(
    `UPDATE vip_subscribers SET status = 'rejected' WHERE city IS NOT NULL AND TRIM(LOWER(city)) = TRIM(LOWER(?))`,
    [city]
  );
  return { count: r.rowsAffected ?? 0 };
}
async function startVipByCity(city, hours) {
  await ensureCityColumn();
  const expiresAt = new Date(Date.now() + hours * 36e5).toISOString();
  const r = await db.execute(
    `UPDATE vip_subscribers SET status = 'active', expires_at = ? WHERE city IS NOT NULL AND TRIM(LOWER(city)) = TRIM(LOWER(?))`,
    [expiresAt, city]
  );
  return { count: r.rowsAffected ?? 0 };
}
async function extendVipByCity(city, hours) {
  await ensureCityColumn();
  const r = await db.execute(
    `UPDATE vip_subscribers SET expires_at = datetime(expires_at, '+' || ? || ' hours') WHERE city IS NOT NULL AND TRIM(LOWER(city)) = TRIM(LOWER(?)) AND status = 'active' AND expires_at IS NOT NULL`,
    [String(hours), city]
  );
  return { count: r.rowsAffected ?? 0 };
}
async function listApprovedByProject(projectId) {
  await ensureColumns();
  const r = await db.execute(
    `SELECT * FROM vip_subscribers WHERE project_id = ? AND status = 'approved' AND expires_at IS NOT NULL ORDER BY created_at DESC`,
    [projectId]
  );
  return rowsToObjects(r).map(decode);
}
async function listActiveByCity(city) {
  await ensureCityColumn();
  const r = await db.execute(
    `SELECT * FROM vip_subscribers
      WHERE status = 'active'
        AND email IS NOT NULL AND TRIM(email) <> ''
        AND city IS NOT NULL AND TRIM(LOWER(city)) = TRIM(LOWER(?))`,
    [city]
  );
  return rowsToObjects(r).map(decode);
}
async function insertVipSubscriber(input) {
  await ensureCityColumn();
  const id = crypto.randomUUID();
  await db.execute(
    `INSERT INTO vip_subscribers (id, name, email, plan, city, status, receipt_key, created_at)
     VALUES (?, ?, ?, ?, ?, 'pending', ?, ?)`,
    [id, input.name, input.email, input.plan, input.city, input.receipt_path, (/* @__PURE__ */ new Date()).toISOString()]
  );
  return id;
}
async function getActiveVipByEmail(email) {
  await ensureCityColumn();
  const r = await db.execute(
    `SELECT * FROM vip_subscribers
      WHERE email IS NOT NULL AND TRIM(LOWER(email)) = TRIM(LOWER(?))
        AND status = 'active'
      ORDER BY created_at DESC LIMIT 1`,
    [email]
  );
  const row = rowsToObjects(r)[0];
  return row ? decode(row) : null;
}
async function createTrialVip(email, durationMinutes) {
  await ensureCityColumn();
  const id = crypto.randomUUID();
  const expiresAt = new Date(Date.now() + durationMinutes * 6e4).toISOString();
  await db.execute(
    `INSERT INTO vip_subscribers (id, name, email, plan, city, status, expires_at, receipt_key, created_at)
     VALUES (?, ?, ?, 'trial', NULL, 'active', ?, NULL, ?)`,
    [id, email, email, expiresAt, (/* @__PURE__ */ new Date()).toISOString()]
  );
  const r = await db.execute(`SELECT * FROM vip_subscribers WHERE id = ? LIMIT 1`, [id]);
  const row = rowsToObjects(r)[0];
  return decode(row);
}
async function findExpiringSoon(hoursWithin) {
  await ensureCityColumn();
  const threshold = new Date(Date.now() + hoursWithin * 36e5).toISOString();
  const r = await db.execute(
    `SELECT * FROM vip_subscribers
      WHERE status = 'active'
        AND expires_at IS NOT NULL
        AND datetime(expires_at) > datetime('now')
        AND datetime(expires_at) <= datetime(?)
      ORDER BY expires_at ASC`,
    [threshold]
  );
  return rowsToObjects(r).map(decode);
}
async function markExpired() {
  await ensureCityColumn();
  const sel = await db.execute(
    `SELECT * FROM vip_subscribers
      WHERE status = 'active' AND expires_at IS NOT NULL AND datetime(expires_at) <= datetime('now')`
  );
  const rows = rowsToObjects(sel).map(decode);
  if (rows.length > 0) {
    await db.execute(
      `UPDATE vip_subscribers SET status = 'expired'
        WHERE status = 'active' AND expires_at IS NOT NULL AND datetime(expires_at) <= datetime('now')`
    );
  }
  return { expired: rows.length, rows };
}
async function autoActivateByCity(city, hours) {
  const projectCity = city.split("-")[0].trim();
  await ensureCityColumn();
  const expiresAt = new Date(Date.now() + hours * 36e5).toISOString();
  const r = await db.execute(
    `UPDATE vip_subscribers SET status = 'active', expires_at = ?
      WHERE city IS NOT NULL AND TRIM(LOWER(city)) = TRIM(LOWER(?))
        AND status IN ('pending', 'approved', 'expired')`,
    [expiresAt, projectCity]
  );
  return { count: r.rowsAffected ?? 0 };
}
async function updateVipReceipt(id, receiptPath) {
  await db.execute(`UPDATE vip_subscribers SET receipt_key = ? WHERE id = ?`, [receiptPath, id]);
}
async function updateVipStatus(id, status) {
  await ensureCityColumn();
  await db.execute(`UPDATE vip_subscribers SET status = ? WHERE id = ?`, [status, id]);
  const r = await db.execute(`SELECT * FROM vip_subscribers WHERE id = ? LIMIT 1`, [id]);
  const row = rowsToObjects(r)[0];
  return row ? decode(row) : null;
}
export {
  approveByProject,
  autoActivateByCity,
  cancelByProject,
  createTrialVip,
  ensureCityColumn,
  extendVipByCity,
  findExpiringSoon,
  getActiveVipByEmail,
  insertVipSubscriber,
  listActiveByCity,
  listAllApprovedWithProject,
  listApprovedByProject,
  listVipSubscribers,
  listVipWithProjectNames,
  markExpired,
  startVipByCity,
  stopVipByCity,
  updateVipReceipt,
  updateVipStatus
};
