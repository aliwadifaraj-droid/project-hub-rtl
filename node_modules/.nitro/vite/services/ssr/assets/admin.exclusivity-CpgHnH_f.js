import { jsxs, jsx, Fragment } from "react/jsx-runtime";
import { u as useServerFn, Z as searchProjectByName, _ as updateExclusivityHours, $ as toggleExclusivityOn, a0 as toggleExclusivityOff } from "./router-CtQuP2fc.js";
import { useQueryClient, useMutation } from "@tanstack/react-query";
import { useState } from "react";
import { Search, Loader2, Lock, Unlock, Clock, Save, Play, Square } from "lucide-react";
import { toast } from "sonner";
import { T as Toaster } from "./sonner-DeNSN9-c.js";
import "@tanstack/react-router";
import "./createSsrRpc-DY9HpWEz.js";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "./auth-middleware.server-B9hAjfqi.js";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
import "zod";
import "./vip.repo-CycBrLVA.js";
import "./saudi-cities-D2sGDQV3.js";
import "@aws-sdk/client-s3";
import "./r2-CJ2zxhhj.js";
import "aws4fetch";
import "@lovable.dev/webhooks-js";
import "@react-email/components";
import "@lovable.dev/email-js";
function ExclusivityPage() {
  const qc = useQueryClient();
  const searchFn = useServerFn(searchProjectByName);
  const updateHoursFn = useServerFn(updateExclusivityHours);
  const toggleOnFn = useServerFn(toggleExclusivityOn);
  const toggleOffFn = useServerFn(toggleExclusivityOff);
  const [query, setQuery] = useState("");
  const [searching, setSearching] = useState(false);
  const [results, setResults] = useState([]);
  const [hoursMap, setHoursMap] = useState({});
  async function handleSearch(e) {
    e.preventDefault();
    if (!query.trim()) return;
    setSearching(true);
    try {
      const rows = await searchFn({
        data: {
          q: query.trim()
        }
      });
      setResults(rows);
      const map = {};
      for (const r of rows) map[r.id] = String(r.exclusive_hours ?? 6);
      setHoursMap(map);
    } catch {
      toast.error("تعذر البحث، حاول مرة أخرى.");
    } finally {
      setSearching(false);
    }
  }
  const saveHoursMut = useMutation({
    mutationFn: (vars) => updateHoursFn({
      data: {
        projectId: vars.projectId,
        hours: vars.hours
      }
    }),
    onSuccess: () => {
      toast.success("تم حفظ عدد الساعات");
      qc.invalidateQueries({
        queryKey: ["exclusivity-search"]
      });
    },
    onError: (e) => toast.error(e.message)
  });
  const toggleOnMut = useMutation({
    mutationFn: (vars) => toggleOnFn({
      data: {
        projectId: vars.projectId,
        hours: vars.hours
      }
    }),
    onSuccess: () => {
      toast.success("تم تشغيل الحصرية");
      refreshResults();
    },
    onError: (e) => toast.error(e.message)
  });
  const toggleOffMut = useMutation({
    mutationFn: (projectId) => toggleOffFn({
      data: {
        projectId
      }
    }),
    onSuccess: () => {
      toast.success("تم إيقاف الحصرية");
      refreshResults();
    },
    onError: (e) => toast.error(e.message)
  });
  async function refreshResults() {
    if (!query.trim()) return;
    try {
      const rows = await searchFn({
        data: {
          q: query.trim()
        }
      });
      setResults(rows);
      const map = {};
      for (const r of rows) map[r.id] = String(r.exclusive_hours ?? 6);
      setHoursMap(map);
    } catch {
    }
  }
  return /* @__PURE__ */ jsxs("div", { dir: "rtl", children: [
    /* @__PURE__ */ jsx(Toaster, { position: "top-center", dir: "rtl" }),
    /* @__PURE__ */ jsx("h1", { className: "text-2xl font-bold mb-2", children: "لوحة تحكم الحصرية" }),
    /* @__PURE__ */ jsx("p", { className: "text-sm text-muted-foreground mb-6", children: "ابحث عن مشروع لتعديل ساعات الحصرية الافتراضية أو تشغيل/إيقاف الحصرية يدوياً." }),
    /* @__PURE__ */ jsxs("form", { onSubmit: handleSearch, className: "flex gap-2 mb-6", children: [
      /* @__PURE__ */ jsxs("div", { className: "relative flex-1 max-w-md", children: [
        /* @__PURE__ */ jsx(Search, { className: "absolute right-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" }),
        /* @__PURE__ */ jsx("input", { type: "text", value: query, onChange: (e) => setQuery(e.target.value), placeholder: "ابحث باسم المشروع...", className: "w-full rounded-lg border border-input bg-background pr-10 pl-4 py-2.5 text-sm outline-none focus:ring-2 focus:ring-ring" })
      ] }),
      /* @__PURE__ */ jsxs("button", { type: "submit", disabled: searching || !query.trim(), className: "inline-flex items-center gap-2 rounded-lg bg-primary px-5 py-2.5 text-sm font-bold text-primary-foreground hover:bg-primary/90 disabled:opacity-60", children: [
        searching ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }) : /* @__PURE__ */ jsx(Search, { className: "h-4 w-4" }),
        "بحث"
      ] })
    ] }),
    results.length === 0 ? /* @__PURE__ */ jsx("div", { className: "rounded-xl border border-border bg-card p-8 text-center text-sm text-muted-foreground", children: "لا توجد نتائج. ابحث عن مشروع بالاسم." }) : /* @__PURE__ */ jsx("div", { className: "space-y-3", children: results.map((r) => {
      const hours = hoursMap[r.id] ?? String(r.exclusive_hours ?? 6);
      const busy = saveHoursMut.isPending || toggleOnMut.isPending || toggleOffMut.isPending;
      return /* @__PURE__ */ jsx("div", { className: "rounded-xl border border-border bg-card p-4 shadow-sm", children: /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-start justify-between gap-3", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex-1 min-w-0", children: [
          /* @__PURE__ */ jsx("h3", { className: "font-bold text-base truncate", children: r.name }),
          r.location ? /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: r.location }) : null,
          /* @__PURE__ */ jsxs("div", { className: "mt-2 flex flex-wrap items-center gap-2", children: [
            /* @__PURE__ */ jsx("span", { className: `inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-xs font-semibold ${r.active ? "bg-amber-100 text-amber-800" : "bg-green-100 text-green-800"}`, children: r.active ? /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(Lock, { className: "h-3 w-3" }),
              " حصري - متبقي ",
              r.remaining_hours,
              " ساعة"
            ] }) : /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsx(Unlock, { className: "h-3 w-3" }),
              " مفتوح للجميع"
            ] }) }),
            r.vip_end_at ? /* @__PURE__ */ jsxs("span", { className: "inline-flex items-center gap-1 text-xs text-muted-foreground", children: [
              /* @__PURE__ */ jsx(Clock, { className: "h-3 w-3" }),
              "ينتهي: ",
              new Date(r.vip_end_at).toLocaleString("ar")
            ] }) : null
          ] })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-center gap-2", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1.5", children: [
            /* @__PURE__ */ jsx("input", { type: "number", min: 1, max: 720, value: hours, onChange: (e) => setHoursMap((m) => ({
              ...m,
              [r.id]: e.target.value
            })), className: "w-20 rounded-md border border-border bg-background px-2 py-1.5 text-sm text-center" }),
            /* @__PURE__ */ jsx("span", { className: "text-xs text-muted-foreground", children: "ساعة" })
          ] }),
          /* @__PURE__ */ jsxs("button", { onClick: () => {
            const n = Number(hours);
            if (!Number.isFinite(n) || n < 1 || n > 720) {
              toast.error("الساعات يجب أن تكون بين 1 و 720");
              return;
            }
            saveHoursMut.mutate({
              projectId: r.id,
              hours: n
            });
          }, disabled: busy, className: "inline-flex items-center gap-1.5 rounded-md bg-blue-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-blue-700 disabled:opacity-60", children: [
            /* @__PURE__ */ jsx(Save, { className: "h-3.5 w-3.5" }),
            " حفظ"
          ] }),
          /* @__PURE__ */ jsxs("button", { onClick: () => {
            const n = Number(hours);
            const h = Number.isFinite(n) && n > 0 ? n : 6;
            toggleOnMut.mutate({
              projectId: r.id,
              hours: h
            });
          }, disabled: busy, className: "inline-flex items-center gap-1.5 rounded-md bg-green-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-green-700 disabled:opacity-60", children: [
            /* @__PURE__ */ jsx(Play, { className: "h-3.5 w-3.5" }),
            " تشغيل"
          ] }),
          /* @__PURE__ */ jsxs("button", { onClick: () => toggleOffMut.mutate(r.id), disabled: busy, className: "inline-flex items-center gap-1.5 rounded-md bg-red-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-red-700 disabled:opacity-60", children: [
            /* @__PURE__ */ jsx(Square, { className: "h-3.5 w-3.5" }),
            " إيقاف"
          ] })
        ] })
      ] }) }, r.id);
    }) })
  ] });
}
export {
  ExclusivityPage as component
};
