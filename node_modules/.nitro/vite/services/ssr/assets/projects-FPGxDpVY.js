import { jsxs, jsx, Fragment } from "react/jsx-runtime";
import { u as useServerFn, h as hasAdminRole, d as listProjects, g as getMe, e as upsertProject, f as deleteProject } from "./router-CtQuP2fc.js";
import { useQueryClient, useQuery, useMutation } from "@tanstack/react-query";
import { useState, useEffect } from "react";
import { u as uploadFile } from "./files.functions-BOtEkJh5.js";
import { b as buildR2Url } from "./projects-SbdB-UfH.js";
import { S as SiteHeader } from "./site-header-DfiCN8H8.js";
import { S as SiteFooter } from "./site-footer-ByXRtXvu.js";
import { T as Toaster } from "./sonner-DeNSN9-c.js";
import { Plus, Loader2, Pencil, Trash2, Check, Share2, X, Upload, Copy } from "lucide-react";
import { toast } from "sonner";
import "@tanstack/react-router";
import "./createSsrRpc-DY9HpWEz.js";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "./auth-middleware.server-B9hAjfqi.js";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
import "zod";
import "./vip.repo-CycBrLVA.js";
import "./saudi-cities-D2sGDQV3.js";
import "@aws-sdk/client-s3";
import "./r2-CJ2zxhhj.js";
import "aws4fetch";
import "@lovable.dev/webhooks-js";
import "@react-email/components";
import "@lovable.dev/email-js";
import "./notifications.functions-mf2-5xG4.js";
import "./chat.functions-DV_-1jmL.js";
function ProjectsPage() {
  const list = useServerFn(listProjects);
  const upsert = useServerFn(upsertProject);
  const del = useServerFn(deleteProject);
  const meFn = useServerFn(getMe);
  const qc = useQueryClient();
  const {
    data,
    isLoading
  } = useQuery({
    queryKey: ["projects"],
    queryFn: () => list()
  });
  const [isAdmin, setIsAdmin] = useState(false);
  const [signedIn, setSignedIn] = useState(false);
  const [editing, setEditing] = useState(null);
  const [sharedId, setSharedId] = useState(null);
  useEffect(() => {
    let cancelled = false;
    meFn().then((me) => {
      if (!me || cancelled) return;
      setSignedIn(true);
      setIsAdmin(hasAdminRole(me.roles));
    }).catch(() => void 0);
    return () => {
      cancelled = true;
    };
  }, [meFn]);
  const saveMut = useMutation({
    mutationFn: (v) => upsert({
      data: v
    }),
    onSuccess: (res, vars) => {
      if (res?.admin_approval === "pending") {
        toast.success("تم إرسال المشروع للمراجعة من الأدمن");
      } else {
        toast.success("تم الحفظ");
      }
      qc.invalidateQueries({
        queryKey: ["projects"]
      });
      qc.invalidateQueries({
        queryKey: ["admin-projects"]
      });
      setEditing(null);
      if (!vars.id && res?.id && res?.admin_approval !== "pending") {
        setSharedId(res.id);
      }
    },
    onError: (e) => toast.error(e.message)
  });
  const delMut = useMutation({
    mutationFn: (id) => del({
      data: {
        id
      }
    }),
    onSuccess: () => {
      toast.success("تم الحذف");
      qc.invalidateQueries({
        queryKey: ["projects"]
      });
      qc.invalidateQueries({
        queryKey: ["admin-projects"]
      });
    },
    onError: (e) => toast.error(e.message)
  });
  return /* @__PURE__ */ jsxs("div", { className: "min-h-screen", children: [
    /* @__PURE__ */ jsx(SiteHeader, {}),
    /* @__PURE__ */ jsx(Toaster, { position: "top-center", dir: "rtl" }),
    /* @__PURE__ */ jsxs("main", { className: "container mx-auto px-4 py-10", children: [
      /* @__PURE__ */ jsxs("div", { className: "mb-6 flex items-center justify-between", children: [
        /* @__PURE__ */ jsxs("h1", { className: "text-2xl font-bold", children: [
          "المشاريع (",
          data?.length ?? 0,
          ")"
        ] }),
        signedIn ? /* @__PURE__ */ jsxs("button", { onClick: () => setEditing({
          name: "",
          description: "",
          location: "",
          duration: "",
          cover_image: "",
          images: []
        }), className: "inline-flex items-center gap-1.5 rounded-md bg-foreground px-3 py-2 text-sm font-semibold text-background hover:bg-foreground/90", children: [
          /* @__PURE__ */ jsx(Plus, { className: "h-4 w-4" }),
          " مشروع جديد"
        ] }) : null
      ] }),
      isLoading ? /* @__PURE__ */ jsx("div", { className: "grid place-items-center py-20", children: /* @__PURE__ */ jsx(Loader2, { className: "h-6 w-6 animate-spin" }) }) : /* @__PURE__ */ jsx("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: (data ?? []).map((p) => /* @__PURE__ */ jsxs("div", { className: "overflow-hidden rounded-xl border border-border bg-card", children: [
        (() => {
          const fallback = buildR2Url(p.cover_image);
          const src = p.cover_url || fallback || "";
          return src ? /* @__PURE__ */ jsx("img", { src, alt: p.name, className: "aspect-video w-full object-cover" }) : /* @__PURE__ */ jsx("div", { className: "aspect-video w-full bg-secondary" });
        })(),
        /* @__PURE__ */ jsxs("div", { className: "p-4", children: [
          /* @__PURE__ */ jsx("h3", { className: "font-bold", children: p.name }),
          /* @__PURE__ */ jsxs("p", { className: "mt-1 text-xs text-muted-foreground", children: [
            p.location,
            " • ",
            p.duration
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "mt-3 flex flex-wrap gap-2", children: [
            /* @__PURE__ */ jsx(ShareLinkButton, { id: p.id }),
            isAdmin ? /* @__PURE__ */ jsxs(Fragment, { children: [
              /* @__PURE__ */ jsxs("button", { onClick: () => setEditing(p), className: "inline-flex items-center justify-center gap-1 rounded-md border border-border px-3 py-1.5 text-xs hover:bg-secondary", children: [
                /* @__PURE__ */ jsx(Pencil, { className: "h-3.5 w-3.5" }),
                " تعديل"
              ] }),
              /* @__PURE__ */ jsxs("button", { onClick: () => {
                if (confirm("تأكيد الحذف؟")) delMut.mutate(p.id);
              }, className: "inline-flex items-center justify-center gap-1 rounded-md border border-destructive/30 px-3 py-1.5 text-xs text-destructive hover:bg-destructive/10", children: [
                /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" }),
                " حذف"
              ] })
            ] }) : null
          ] })
        ] })
      ] }, p.id)) }),
      editing ? /* @__PURE__ */ jsx(ProjectModal, { value: editing, onClose: () => setEditing(null), onSave: (v) => saveMut.mutate(v), saving: saveMut.isPending }) : null,
      sharedId ? /* @__PURE__ */ jsx(SharedLinkModal, { id: sharedId, onClose: () => setSharedId(null) }) : null
    ] }),
    /* @__PURE__ */ jsx(SiteFooter, {})
  ] });
}
function ProjectModal({
  value,
  onClose,
  onSave,
  saving
}) {
  const [form, setForm] = useState(value);
  const [uploading, setUploading] = useState(false);
  const upload = useServerFn(uploadFile);
  async function uploadProjectFile(file) {
    const data = await fileToBase64(file);
    const purpose = file.type === "application/pdf" ? "bid-pdf" : "project-image";
    const res = await upload({
      data: {
        filename: file.name,
        mime: file.type,
        purpose,
        data
      }
    });
    return res.key;
  }
  async function onCover(e) {
    const f = e.target.files?.[0];
    if (!f) return;
    setUploading(true);
    try {
      const path = await uploadProjectFile(f);
      setForm((s) => ({
        ...s,
        cover_image: path
      }));
      toast.success("تم رفع صورة الغلاف");
    } catch (err) {
      toast.error("فشل الرفع");
      console.error(err);
    } finally {
      setUploading(false);
    }
  }
  async function onGallery(e) {
    const files = Array.from(e.target.files ?? []);
    if (files.length === 0) return;
    setUploading(true);
    try {
      const paths = await Promise.all(files.map(uploadProjectFile));
      setForm((s) => ({
        ...s,
        images: [...s.images ?? [], ...paths]
      }));
    } catch {
      toast.error("فشل رفع الصور");
    } finally {
      setUploading(false);
    }
  }
  async function onPdf(e) {
    const f = e.target.files?.[0];
    if (!f) return;
    if (f.type !== "application/pdf") {
      toast.error("الملف يجب أن يكون PDF");
      return;
    }
    setUploading(true);
    try {
      const path = await uploadProjectFile(f);
      setForm((s) => ({
        ...s,
        pdf_file: path
      }));
      toast.success("تم رفع ملف PDF");
    } catch (err) {
      toast.error("فشل رفع PDF");
      console.error(err);
    } finally {
      setUploading(false);
    }
  }
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-50 grid place-items-center bg-black/50 p-4", onClick: onClose, children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-2xl rounded-2xl bg-card p-6 shadow-xl max-h-[90vh] overflow-auto", onClick: (e) => e.stopPropagation(), children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-4 flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-xl font-bold", children: form.id ? "تعديل مشروع" : "مشروع جديد" }),
      /* @__PURE__ */ jsx("button", { onClick: onClose, className: "rounded-md p-1 hover:bg-secondary", children: /* @__PURE__ */ jsx(X, { className: "h-5 w-5" }) })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "space-y-4", children: [
      /* @__PURE__ */ jsx(Field, { label: "اسم المشروع", children: /* @__PURE__ */ jsx("input", { className: "inp", value: form.name ?? "", onChange: (e) => setForm({
        ...form,
        name: e.target.value
      }) }) }),
      /* @__PURE__ */ jsx(Field, { label: "الوصف", children: /* @__PURE__ */ jsx("textarea", { rows: 4, className: "inp", value: form.description ?? "", onChange: (e) => setForm({
        ...form,
        description: e.target.value
      }) }) }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ jsx(Field, { label: "الموقع", children: /* @__PURE__ */ jsx("input", { className: "inp", value: form.location ?? "", onChange: (e) => setForm({
          ...form,
          location: e.target.value
        }) }) }),
        /* @__PURE__ */ jsx(Field, { label: "المدة المتوقعة", children: /* @__PURE__ */ jsx("input", { className: "inp", value: form.duration ?? "", onChange: (e) => setForm({
          ...form,
          duration: e.target.value
        }) }) })
      ] }),
      /* @__PURE__ */ jsx(Field, { label: "صورة الغلاف", children: /* @__PURE__ */ jsxs("label", { className: "flex cursor-pointer items-center gap-2 rounded-lg border-2 border-dashed border-border bg-secondary/40 px-3 py-3 text-sm hover:bg-secondary", children: [
        /* @__PURE__ */ jsx(Upload, { className: "h-4 w-4" }),
        /* @__PURE__ */ jsx("span", { className: "flex-1 text-muted-foreground truncate", children: form.cover_image || "اختر صورة" }),
        /* @__PURE__ */ jsx("input", { type: "file", accept: "image/*", className: "hidden", onChange: onCover })
      ] }) }),
      /* @__PURE__ */ jsx(Field, { label: `معرض الصور (${form.images?.length ?? 0})`, children: /* @__PURE__ */ jsxs("label", { className: "flex cursor-pointer items-center gap-2 rounded-lg border-2 border-dashed border-border bg-secondary/40 px-3 py-3 text-sm hover:bg-secondary", children: [
        /* @__PURE__ */ jsx(Upload, { className: "h-4 w-4" }),
        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: "إضافة صور" }),
        /* @__PURE__ */ jsx("input", { type: "file", accept: "image/*", multiple: true, className: "hidden", onChange: onGallery })
      ] }) }),
      /* @__PURE__ */ jsxs(Field, { label: "ملف PDF (اختياري)", children: [
        /* @__PURE__ */ jsxs("label", { className: "flex cursor-pointer items-center gap-2 rounded-lg border-2 border-dashed border-border bg-secondary/40 px-3 py-3 text-sm hover:bg-secondary", children: [
          /* @__PURE__ */ jsx(Upload, { className: "h-4 w-4" }),
          /* @__PURE__ */ jsx("span", { className: "text-muted-foreground truncate", children: form.pdf_file || "اختر ملف PDF" }),
          /* @__PURE__ */ jsx("input", { type: "file", accept: "application/pdf", className: "hidden", onChange: onPdf })
        ] }),
        form.pdf_file ? /* @__PURE__ */ jsx("button", { type: "button", onClick: () => setForm((s) => ({
          ...s,
          pdf_file: null
        })), className: "mt-1 text-xs text-destructive hover:underline", children: "إزالة الملف" }) : null
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "mt-6 flex gap-2", children: [
      /* @__PURE__ */ jsxs("button", { disabled: saving || uploading || !form.name || !form.cover_image, onClick: () => onSave(form), className: "flex-1 inline-flex items-center justify-center gap-2 rounded-md bg-foreground px-4 py-2.5 text-sm font-semibold text-background hover:bg-foreground/90 disabled:opacity-60", children: [
        saving || uploading ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }) : null,
        " حفظ"
      ] }),
      /* @__PURE__ */ jsx("button", { onClick: onClose, className: "rounded-md border border-border px-4 py-2.5 text-sm hover:bg-secondary", children: "إلغاء" })
    ] })
  ] }) });
}
function Field({
  label,
  children
}) {
  return /* @__PURE__ */ jsxs("div", { children: [
    /* @__PURE__ */ jsx("label", { className: "mb-1.5 block text-sm font-semibold", children: label }),
    children
  ] });
}
function fileToBase64(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve(String(reader.result));
    reader.onerror = reject;
    reader.readAsDataURL(file);
  });
}
function buildProjectUrl(id) {
  const origin = typeof window !== "undefined" ? window.location.origin : "";
  return `${origin}/project/${id}`;
}
async function copyText(text) {
  try {
    if (navigator.clipboard?.writeText) {
      await navigator.clipboard.writeText(text);
      return true;
    }
  } catch {
  }
  try {
    const ta = document.createElement("textarea");
    ta.value = text;
    ta.style.position = "fixed";
    ta.style.opacity = "0";
    document.body.appendChild(ta);
    ta.select();
    document.execCommand("copy");
    document.body.removeChild(ta);
    return true;
  } catch {
    return false;
  }
}
function ShareLinkButton({
  id
}) {
  const [copied, setCopied] = useState(false);
  return /* @__PURE__ */ jsxs("button", { onClick: async () => {
    const ok = await copyText(buildProjectUrl(id));
    if (ok) {
      setCopied(true);
      toast.success("تم نسخ الرابط");
      setTimeout(() => setCopied(false), 2e3);
    } else toast.error("تعذر النسخ");
  }, className: "inline-flex flex-1 items-center justify-center gap-1 rounded-md border border-border px-3 py-1.5 text-xs hover:bg-secondary", children: [
    copied ? /* @__PURE__ */ jsx(Check, { className: "h-3.5 w-3.5 text-accent" }) : /* @__PURE__ */ jsx(Share2, { className: "h-3.5 w-3.5" }),
    copied ? "تم النسخ" : "نسخ الرابط"
  ] });
}
function SharedLinkModal({
  id,
  onClose
}) {
  const url = buildProjectUrl(id);
  const [copied, setCopied] = useState(false);
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-50 grid place-items-center bg-black/50 p-4", onClick: onClose, children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-md rounded-2xl bg-card p-6 shadow-xl", onClick: (e) => e.stopPropagation(), children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-4 flex items-center justify-between", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-lg font-bold", children: "تم نشر المشروع 🎉" }),
      /* @__PURE__ */ jsx("button", { onClick: onClose, className: "rounded-md p-1 hover:bg-secondary", children: /* @__PURE__ */ jsx(X, { className: "h-5 w-5" }) })
    ] }),
    /* @__PURE__ */ jsx("p", { className: "mb-3 text-sm text-muted-foreground", children: "شارك هذا الرابط مع عملائك:" }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 rounded-lg border border-border bg-secondary/40 p-2", children: [
      /* @__PURE__ */ jsx("input", { readOnly: true, value: url, className: "flex-1 bg-transparent px-2 py-1 text-sm outline-none", dir: "ltr" }),
      /* @__PURE__ */ jsxs("button", { onClick: async () => {
        const ok = await copyText(url);
        if (ok) {
          setCopied(true);
          toast.success("تم نسخ الرابط");
          setTimeout(() => setCopied(false), 2e3);
        } else toast.error("تعذر النسخ");
      }, className: "inline-flex items-center gap-1 rounded-md bg-foreground px-3 py-1.5 text-xs font-semibold text-background hover:bg-foreground/90", children: [
        copied ? /* @__PURE__ */ jsx(Check, { className: "h-3.5 w-3.5" }) : /* @__PURE__ */ jsx(Copy, { className: "h-3.5 w-3.5" }),
        copied ? "تم" : "نسخ"
      ] })
    ] }),
    /* @__PURE__ */ jsx("button", { onClick: onClose, className: "mt-5 w-full rounded-md border border-border px-4 py-2 text-sm hover:bg-secondary", children: "إغلاق" })
  ] }) });
}
export {
  ProjectsPage as component
};
