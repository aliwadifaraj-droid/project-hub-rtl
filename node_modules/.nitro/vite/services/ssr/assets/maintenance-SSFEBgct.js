import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { u as useServerFn, b as getMaintenance } from "./router-CtQuP2fc.js";
import { useState, useEffect } from "react";
import { Wrench } from "lucide-react";
import "@tanstack/react-router";
import "./createSsrRpc-DY9HpWEz.js";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "./auth-middleware.server-B9hAjfqi.js";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
import "zod";
import "./vip.repo-CycBrLVA.js";
import "./saudi-cities-D2sGDQV3.js";
import "@aws-sdk/client-s3";
import "./r2-CJ2zxhhj.js";
import "aws4fetch";
import "@lovable.dev/webhooks-js";
import "@react-email/components";
import "@lovable.dev/email-js";
function useCountdown(target) {
  const [now, setNow] = useState(() => Date.now());
  useEffect(() => {
    const t = setInterval(() => setNow(Date.now()), 1e3);
    return () => clearInterval(t);
  }, []);
  if (!target) return null;
  const end = new Date(target).getTime();
  if (Number.isNaN(end)) return null;
  const diff = Math.max(0, end - now);
  const s = Math.floor(diff / 1e3);
  return {
    days: Math.floor(s / 86400),
    hours: Math.floor(s % 86400 / 3600),
    minutes: Math.floor(s % 3600 / 60),
    seconds: s % 60,
    done: diff === 0
  };
}
function MaintenancePage() {
  const fetchMaintenance = useServerFn(getMaintenance);
  const {
    data
  } = useQuery({
    queryKey: ["maintenance-public"],
    queryFn: () => fetchMaintenance(),
    refetchInterval: 3e4
  });
  const cd = useCountdown(data?.endAt ?? null);
  return /* @__PURE__ */ jsx("div", { className: "min-h-screen bg-secondary/40 flex items-center justify-center px-4", dir: "rtl", children: /* @__PURE__ */ jsxs("div", { className: "max-w-lg w-full rounded-2xl border border-border bg-card p-8 text-center shadow-sm", children: [
    /* @__PURE__ */ jsx("div", { className: "mx-auto grid h-16 w-16 place-items-center rounded-full bg-primary/10 text-primary", children: /* @__PURE__ */ jsx(Wrench, { className: "h-8 w-8" }) }),
    /* @__PURE__ */ jsx("h1", { className: "mt-4 text-2xl font-bold", children: "الموقع تحت الصيانة" }),
    /* @__PURE__ */ jsx("p", { className: "mt-2 text-muted-foreground text-sm", children: "نعمل حاليًا على تحسين الخدمة. نعتذر عن الإزعاج وسنعود قريبًا بإذن الله." }),
    data?.endAt && cd && !cd.done && /* @__PURE__ */ jsxs("div", { className: "mt-6", children: [
      /* @__PURE__ */ jsx("div", { className: "text-xs text-muted-foreground mb-2", children: "الوقت المتبقي" }),
      /* @__PURE__ */ jsx("div", { className: "grid grid-cols-4 gap-2", children: [{
        l: "يوم",
        v: cd.days
      }, {
        l: "ساعة",
        v: cd.hours
      }, {
        l: "دقيقة",
        v: cd.minutes
      }, {
        l: "ثانية",
        v: cd.seconds
      }].map((x) => /* @__PURE__ */ jsxs("div", { className: "rounded-lg bg-secondary p-3", children: [
        /* @__PURE__ */ jsx("div", { className: "text-2xl font-bold tabular-nums", children: String(x.v).padStart(2, "0") }),
        /* @__PURE__ */ jsx("div", { className: "text-[10px] text-muted-foreground mt-1", children: x.l })
      ] }, x.l)) }),
      /* @__PURE__ */ jsxs("div", { className: "mt-3 text-xs text-muted-foreground", children: [
        "العودة المتوقعة: ",
        new Date(data.endAt).toLocaleString("ar")
      ] })
    ] }),
    data?.endAt && cd?.done && /* @__PURE__ */ jsx("div", { className: "mt-6 text-sm text-primary", children: "انتهى وقت الصيانة، يمكنك تحديث الصفحة الآن." })
  ] }) });
}
export {
  MaintenancePage as component
};
