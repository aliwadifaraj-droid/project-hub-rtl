import { jsx, jsxs, Fragment } from "react/jsx-runtime";
import { u as useServerFn, ac as getPlatformRequests, ad as updateRequestStatus, ae as getBidPdfUrl, k as getMyRoles, af as adminListProjectOfferToggles, ag as adminSetProjectOffersEnabled, ah as adminSetAllProjectOffersEnabled, ai as adminSetProjectBotOffersEnabled, aj as adminSetAllProjectBotOffersEnabled, ak as sendRequestMessage } from "./router-CtQuP2fc.js";
import { useQueryClient, useQuery, useMutation } from "@tanstack/react-query";
import { useState } from "react";
import { Loader2, Bell, FileDown, Mail, Ban, ToggleRight, ToggleLeft, Bot, BotOff, X } from "lucide-react";
import { toast } from "sonner";
import { a as adminBlockCompany } from "./blocked.functions-pr1NkNVd.js";
import "@tanstack/react-router";
import "./createSsrRpc-DY9HpWEz.js";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "./auth-middleware.server-B9hAjfqi.js";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
import "zod";
import "./vip.repo-CycBrLVA.js";
import "./saudi-cities-D2sGDQV3.js";
import "@aws-sdk/client-s3";
import "./r2-CJ2zxhhj.js";
import "aws4fetch";
import "@lovable.dev/webhooks-js";
import "@react-email/components";
import "@lovable.dev/email-js";
const STATUS = {
  pending: {
    label: "قيد الانتظار",
    cls: "bg-gray-500/15 text-gray-700 dark:text-gray-300"
  },
  new: {
    label: "جديد",
    cls: "bg-blue-500/15 text-blue-700 dark:text-blue-300"
  },
  reviewing: {
    label: "قيد المراجعة",
    cls: "bg-amber-500/15 text-amber-700 dark:text-amber-300"
  },
  accepted: {
    label: "مقبول",
    cls: "bg-emerald-500/15 text-emerald-700 dark:text-emerald-300"
  },
  rejected: {
    label: "مرفوض",
    cls: "bg-red-500/15 text-red-700 dark:text-red-300"
  }
};
function RequestsPage() {
  const list = useServerFn(getPlatformRequests);
  const update = useServerFn(updateRequestStatus);
  const getUrl = useServerFn(getBidPdfUrl);
  const getRoles = useServerFn(getMyRoles);
  const qc = useQueryClient();
  const {
    data,
    isLoading
  } = useQuery({
    queryKey: ["platform-requests"],
    queryFn: () => list()
  });
  const {
    data: roles
  } = useQuery({
    queryKey: ["my-roles"],
    queryFn: () => getRoles()
  });
  const isAdmin = roles?.includes("admin");
  const [msgTarget, setMsgTarget] = useState(null);
  const [noteTarget, setNoteTarget] = useState(null);
  const blockFn = useServerFn(adminBlockCompany);
  const blockMut = useMutation({
    mutationFn: (v) => blockFn({
      data: v
    }),
    onSuccess: () => {
      toast.success("تم حظر الشركة");
      qc.invalidateQueries({
        queryKey: ["platform-requests"]
      });
    },
    onError: (e) => toast.error(e.message)
  });
  const mut = useMutation({
    mutationFn: (v) => update({
      data: v
    }),
    onSuccess: () => {
      toast.success("تم تحديث الحالة");
      setNoteTarget(null);
      qc.invalidateQueries({
        queryKey: ["platform-requests"]
      });
    },
    onError: (e) => toast.error(e.message)
  });
  async function openPdf(path) {
    try {
      const url = await getUrl({
        data: {
          path
        }
      });
      window.open(url, "_blank");
    } catch {
      toast.error("تعذر فتح الملف");
    }
  }
  if (isLoading) return /* @__PURE__ */ jsx("div", { className: "grid place-items-center py-20", children: /* @__PURE__ */ jsx(Loader2, { className: "h-6 w-6 animate-spin" }) });
  const rows = data ?? [];
  const newCount = rows.filter((r) => r.status === "new" || r.status === "pending").length;
  return /* @__PURE__ */ jsxs("div", { dir: "rtl", children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-4 flex items-center justify-between gap-3", children: [
      /* @__PURE__ */ jsxs("h1", { className: "text-xl md:text-2xl font-bold", children: [
        "الطلبات الواردة (",
        rows.length,
        ")"
      ] }),
      /* @__PURE__ */ jsxs("button", { "aria-label": "طلبات جديدة", className: "relative inline-flex h-10 w-10 items-center justify-center rounded-full border border-slate-700 bg-slate-800 text-slate-200 hover:bg-slate-700 transition", children: [
        /* @__PURE__ */ jsx(Bell, { className: "h-5 w-5" }),
        newCount > 0 && /* @__PURE__ */ jsx("span", { className: "absolute -top-1.5 -start-1.5 grid min-h-5 min-w-5 place-items-center rounded-full bg-red-500 px-1 text-[10px] font-bold text-white", children: newCount > 99 ? "99+" : newCount })
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "overflow-hidden rounded-xl border border-slate-700 bg-slate-900 text-slate-100 shadow-lg", children: [
      /* @__PURE__ */ jsx("div", { className: "hidden md:block overflow-x-auto", children: /* @__PURE__ */ jsxs("table", { className: "w-full text-right text-sm", children: [
        /* @__PURE__ */ jsx("thead", { className: "bg-slate-800 text-slate-200", children: /* @__PURE__ */ jsxs("tr", { children: [
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "الشركة" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "المشروع" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "البريد" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "موقع المنشأة" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "التاريخ" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "الحالة" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "عرض السعر" }),
          /* @__PURE__ */ jsx("th", { className: "p-3 font-semibold", children: "حظر" })
        ] }) }),
        /* @__PURE__ */ jsxs("tbody", { children: [
          rows.map((r) => /* @__PURE__ */ jsxs("tr", { className: "border-t border-slate-800 hover:bg-slate-800/50", children: [
            /* @__PURE__ */ jsx("td", { className: "p-3 font-medium", children: r.company_name }),
            /* @__PURE__ */ jsxs("td", { className: "p-3 text-slate-300", children: [
              /* @__PURE__ */ jsx("div", { children: r.projects?.name ?? "-" }),
              r.submitter_type && /* @__PURE__ */ jsx(SubmitterBadge, { type: r.submitter_type })
            ] }),
            /* @__PURE__ */ jsx("td", { className: "p-3 text-slate-300 ltr text-left", dir: "ltr", children: r.email ? /* @__PURE__ */ jsx("a", { href: `mailto:${r.email}`, className: "text-blue-300 hover:underline", children: r.email }) : "-" }),
            /* @__PURE__ */ jsxs("td", { className: "p-3 text-slate-300", children: [
              /* @__PURE__ */ jsx("div", { children: r.facility_location }),
              r.note ? /* @__PURE__ */ jsxs("div", { className: "mt-1 text-xs text-amber-300", children: [
                "📝 ",
                r.note
              ] }) : null
            ] }),
            /* @__PURE__ */ jsx("td", { className: "p-3 text-slate-400 text-xs", children: new Date(r.created_at).toLocaleDateString("ar") }),
            /* @__PURE__ */ jsx("td", { className: "p-3", children: isAdmin || r.can_manage ? /* @__PURE__ */ jsx("select", { value: r.status, onChange: (e) => setNoteTarget({
              id: r.id,
              status: e.target.value,
              note: r.note ?? ""
            }), className: `rounded-md border border-slate-600 bg-slate-800 px-2 py-1 text-xs font-medium ${STATUS[r.status].cls}`, children: Object.entries(STATUS).map(([k, v]) => /* @__PURE__ */ jsx("option", { value: k, className: "bg-slate-800 text-slate-100", children: v.label }, k)) }) : /* @__PURE__ */ jsx("span", { className: `inline-block rounded-full px-3 py-1 text-xs font-semibold ${STATUS[r.status].cls}`, children: STATUS[r.status].label }) }),
            /* @__PURE__ */ jsx("td", { className: "p-3", children: /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
              isAdmin || r.can_manage ? /* @__PURE__ */ jsxs("button", { onClick: () => openPdf(r.pdf_url ?? ""), className: "inline-flex items-center gap-1 rounded-md bg-slate-700 px-2.5 py-1.5 text-xs font-medium text-slate-100 hover:bg-slate-600", children: [
                /* @__PURE__ */ jsx(FileDown, { className: "h-4 w-4" }),
                " فتح PDF"
              ] }) : /* @__PURE__ */ jsxs("span", { className: "inline-flex items-center gap-1 rounded-md bg-slate-700/50 px-2.5 py-1.5 text-xs font-medium text-slate-400 cursor-not-allowed", children: [
                /* @__PURE__ */ jsx(FileDown, { className: "h-4 w-4" }),
                " غير مصرح"
              ] }),
              (isAdmin || r.can_manage) && r.email ? /* @__PURE__ */ jsxs("button", { onClick: () => setMsgTarget({
                email: String(r.email),
                company: String(r.company_name ?? "")
              }), className: "inline-flex items-center gap-1 rounded-md bg-blue-600 px-2.5 py-1.5 text-xs font-medium text-white hover:bg-blue-500", children: [
                /* @__PURE__ */ jsx(Mail, { className: "h-4 w-4" }),
                " رسالة خاصة"
              ] }) : null
            ] }) }),
            /* @__PURE__ */ jsx("td", { className: "p-3", children: isAdmin ? /* @__PURE__ */ jsxs("button", { disabled: blockMut.isPending, onClick: () => blockMut.mutate({
              company_name: r.company_name ?? "",
              email: r.email ?? ""
            }), className: "inline-flex items-center gap-1 rounded-md bg-red-600/80 px-2.5 py-1.5 text-xs font-medium text-white hover:bg-red-500 disabled:opacity-60", children: [
              /* @__PURE__ */ jsx(Ban, { className: "h-4 w-4" }),
              " حظر"
            ] }) : "-" })
          ] }, r.id)),
          rows.length === 0 && /* @__PURE__ */ jsx("tr", { children: /* @__PURE__ */ jsx("td", { colSpan: 8, className: "p-8 text-center text-slate-400", children: "لا توجد طلبات بعد" }) })
        ] })
      ] }) }),
      /* @__PURE__ */ jsxs("div", { className: "md:hidden divide-y divide-slate-800", children: [
        rows.map((r) => /* @__PURE__ */ jsxs("div", { className: "p-4 space-y-2", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-start justify-between gap-2", children: [
            /* @__PURE__ */ jsxs("div", { children: [
              /* @__PURE__ */ jsx("div", { className: "font-bold", children: r.company_name }),
              /* @__PURE__ */ jsx("div", { className: "text-xs text-slate-400 mt-0.5", children: r.projects?.name ?? "-" }),
              r.submitter_type && /* @__PURE__ */ jsx(SubmitterBadge, { type: r.submitter_type })
            ] }),
            isAdmin || r.can_manage ? /* @__PURE__ */ jsx("select", { value: r.status, onChange: (e) => setNoteTarget({
              id: r.id,
              status: e.target.value,
              note: r.note ?? ""
            }), className: `shrink-0 rounded-md border border-slate-600 bg-slate-800 px-2 py-1 text-xs font-medium ${STATUS[r.status].cls}`, children: Object.entries(STATUS).map(([k, v]) => /* @__PURE__ */ jsx("option", { value: k, className: "bg-slate-800 text-slate-100", children: v.label }, k)) }) : /* @__PURE__ */ jsx("span", { className: `shrink-0 rounded-full px-3 py-1 text-xs font-semibold ${STATUS[r.status].cls}`, children: STATUS[r.status].label })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "text-sm text-slate-300", children: [
            "📍 ",
            r.facility_location
          ] }),
          r.note ? /* @__PURE__ */ jsxs("div", { className: "text-xs text-amber-300", children: [
            "📝 ",
            r.note
          ] }) : null,
          r.email && /* @__PURE__ */ jsxs("div", { className: "text-xs text-slate-300 ltr text-left", dir: "ltr", children: [
            "✉️ ",
            /* @__PURE__ */ jsx("a", { href: `mailto:${r.email}`, className: "text-blue-300 hover:underline", children: r.email })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between pt-1", children: [
            /* @__PURE__ */ jsx("span", { className: "text-xs text-slate-500", children: new Date(r.created_at).toLocaleDateString("ar") }),
            isAdmin || r.can_manage ? /* @__PURE__ */ jsxs("button", { onClick: () => openPdf(r.pdf_url ?? ""), className: "inline-flex items-center gap-1 rounded-md bg-slate-700 px-3 py-1.5 text-xs font-medium hover:bg-slate-600", children: [
              /* @__PURE__ */ jsx(FileDown, { className: "h-4 w-4" }),
              " فتح PDF"
            ] }) : /* @__PURE__ */ jsxs("span", { className: "inline-flex items-center gap-1 rounded-md bg-slate-700/50 px-3 py-1.5 text-xs font-medium text-slate-400 cursor-not-allowed", children: [
              /* @__PURE__ */ jsx(FileDown, { className: "h-4 w-4" }),
              " غير مصرح"
            ] })
          ] }),
          (isAdmin || r.can_manage) && r.email ? /* @__PURE__ */ jsxs("button", { onClick: () => setMsgTarget({
            email: String(r.email),
            company: String(r.company_name ?? "")
          }), className: "inline-flex w-full items-center justify-center gap-1 rounded-md bg-blue-600 px-3 py-2 text-xs font-medium text-white hover:bg-blue-500", children: [
            /* @__PURE__ */ jsx(Mail, { className: "h-4 w-4" }),
            " رسالة خاصة"
          ] }) : null
        ] }, r.id)),
        rows.length === 0 && /* @__PURE__ */ jsx("div", { className: "p-8 text-center text-slate-400", children: "لا توجد طلبات بعد" })
      ] })
    ] }),
    isAdmin && /* @__PURE__ */ jsxs(Fragment, { children: [
      /* @__PURE__ */ jsx(OfferTogglesPanel, {}),
      /* @__PURE__ */ jsx(BotOfferTogglesPanel, {})
    ] }),
    noteTarget && /* @__PURE__ */ jsx(NoteModal, { target: noteTarget, required: !isAdmin, pending: mut.isPending, onClose: () => setNoteTarget(null), onSubmit: (note) => mut.mutate({
      id: noteTarget.id,
      status: noteTarget.status,
      note
    }) }),
    msgTarget && /* @__PURE__ */ jsx(MessageModal, { target: msgTarget, onClose: () => setMsgTarget(null) })
  ] });
}
function NoteModal({
  target,
  required,
  pending,
  onClose,
  onSubmit
}) {
  const [note, setNote] = useState(target.note ?? "");
  function submit(e) {
    e.preventDefault();
    if (required && !note.trim()) {
      toast.error("الملاحظة إجبارية");
      return;
    }
    onSubmit(note.trim());
  }
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-50 grid place-items-center bg-black/60 p-4", dir: "rtl", onClick: onClose, children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-lg rounded-xl border border-slate-700 bg-slate-900 p-5 text-slate-100 shadow-xl", onClick: (e) => e.stopPropagation(), children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-3 flex items-start justify-between gap-3", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("h2", { className: "text-lg font-bold", children: "ملاحظة تغيير الحالة" }),
        /* @__PURE__ */ jsxs("p", { className: "mt-0.5 text-xs text-slate-400", children: [
          "الحالة الجديدة: ",
          /* @__PURE__ */ jsx("span", { className: "font-semibold", children: STATUS[target.status].label }),
          " — ",
          required ? "الملاحظة إجبارية" : "الملاحظة اختيارية"
        ] })
      ] }),
      /* @__PURE__ */ jsx("button", { "aria-label": "إغلاق", onClick: onClose, className: "rounded-md p-1 text-slate-400 hover:bg-slate-800 hover:text-slate-100", children: /* @__PURE__ */ jsx(X, { className: "h-5 w-5" }) })
    ] }),
    /* @__PURE__ */ jsxs("form", { onSubmit: submit, className: "space-y-3", children: [
      /* @__PURE__ */ jsx("textarea", { value: note, onChange: (e) => setNote(e.target.value), rows: 5, maxLength: 2e3, placeholder: "اكتب الملاحظة...", className: "w-full resize-y rounded-lg border border-slate-700 bg-slate-800 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500" }),
      /* @__PURE__ */ jsx("p", { className: "text-xs text-slate-500", children: "الملاحظة تظهر للإدارة وفي رد البوت فقط، ولا تُرسل في بريد العميل." }),
      /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2", children: [
        /* @__PURE__ */ jsx("button", { type: "button", onClick: onClose, className: "rounded-md border border-slate-700 px-4 py-2 text-sm hover:bg-slate-800", children: "إلغاء" }),
        /* @__PURE__ */ jsxs("button", { type: "submit", disabled: pending, className: "inline-flex items-center gap-2 rounded-md bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-500 disabled:opacity-60", children: [
          pending ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }) : null,
          " حفظ الحالة"
        ] })
      ] })
    ] })
  ] }) });
}
function OfferTogglesPanel() {
  const listFn = useServerFn(adminListProjectOfferToggles);
  const setOneFn = useServerFn(adminSetProjectOffersEnabled);
  const setAllFn = useServerFn(adminSetAllProjectOffersEnabled);
  const qc = useQueryClient();
  const {
    data: projects = [],
    isLoading
  } = useQuery({
    queryKey: ["project-offer-toggles"],
    queryFn: () => listFn()
  });
  function refresh() {
    qc.invalidateQueries({
      queryKey: ["project-offer-toggles"]
    });
  }
  const toggleOne = useMutation({
    mutationFn: (v) => setOneFn({
      data: v
    }),
    onSuccess: (_d, v) => {
      toast.success(v.enabled ? "تم تفعيل إرسال عرض السعر" : "تم تعطيل إرسال عرض السعر");
      refresh();
    },
    onError: (e) => toast.error(e.message)
  });
  const toggleAll = useMutation({
    mutationFn: (enabled) => setAllFn({
      data: {
        enabled
      }
    }),
    onSuccess: (_d, enabled) => {
      toast.success(enabled ? "تم تفعيل الكل" : "تم تعطيل الكل");
      refresh();
    },
    onError: (e) => toast.error(e.message)
  });
  const busy = toggleOne.isPending || toggleAll.isPending;
  return /* @__PURE__ */ jsxs("section", { className: "mt-6 rounded-xl border border-slate-700 bg-slate-900 p-4 text-slate-100 shadow-lg", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-center justify-between gap-3", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-base md:text-lg font-bold", children: "التحكم في زر «إرسال عرض سعر»" }),
      /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ jsxs("button", { disabled: busy, onClick: () => toggleAll.mutate(true), className: "inline-flex items-center gap-1 rounded-md bg-emerald-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-emerald-500 disabled:opacity-60", children: [
          /* @__PURE__ */ jsx(ToggleRight, { className: "h-4 w-4" }),
          " تفعيل الكل"
        ] }),
        /* @__PURE__ */ jsxs("button", { disabled: busy, onClick: () => toggleAll.mutate(false), className: "inline-flex items-center gap-1 rounded-md bg-red-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-red-500 disabled:opacity-60", children: [
          /* @__PURE__ */ jsx(ToggleLeft, { className: "h-4 w-4" }),
          " تعطيل الكل"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx("p", { className: "mt-1 text-xs text-slate-400", children: "عند التعطيل يختفي زر «إرسال عرض سعر» للعميل في صفحة المشروع ويرفض البوت استلام العروض لهذا المشروع." }),
    isLoading ? /* @__PURE__ */ jsx("div", { className: "grid place-items-center py-8", children: /* @__PURE__ */ jsx(Loader2, { className: "h-5 w-5 animate-spin" }) }) : projects.length === 0 ? /* @__PURE__ */ jsx("div", { className: "py-6 text-center text-sm text-slate-400", children: "لا توجد مشاريع" }) : /* @__PURE__ */ jsx("ul", { className: "mt-3 divide-y divide-slate-800", children: projects.map((p) => /* @__PURE__ */ jsxs("li", { className: "flex items-center justify-between gap-3 py-2.5", children: [
      /* @__PURE__ */ jsx("span", { className: "text-sm font-medium", children: p.name }),
      /* @__PURE__ */ jsx("button", { type: "button", role: "switch", "aria-checked": p.offers_enabled, "aria-label": `تشغيل أو إطفاء عرض السعر لمشروع ${p.name}`, disabled: busy, onClick: () => toggleOne.mutate({
        id: p.id,
        enabled: !p.offers_enabled
      }), className: `relative inline-flex h-6 w-11 shrink-0 items-center rounded-full transition disabled:opacity-60 ${p.offers_enabled ? "bg-emerald-600" : "bg-slate-600"}`, children: /* @__PURE__ */ jsx("span", { className: `absolute h-5 w-5 rounded-full bg-white transition-all ${p.offers_enabled ? "start-[2px]" : "start-[22px]"}` }) })
    ] }, p.id)) })
  ] });
}
function BotOfferTogglesPanel() {
  const listFn = useServerFn(adminListProjectOfferToggles);
  const setOneFn = useServerFn(adminSetProjectBotOffersEnabled);
  const setAllFn = useServerFn(adminSetAllProjectBotOffersEnabled);
  const qc = useQueryClient();
  const {
    data: projects = [],
    isLoading
  } = useQuery({
    queryKey: ["project-offer-toggles"],
    queryFn: () => listFn()
  });
  function refresh() {
    qc.invalidateQueries({
      queryKey: ["project-offer-toggles"]
    });
  }
  const toggleOne = useMutation({
    mutationFn: (v) => setOneFn({
      data: v
    }),
    onSuccess: (_d, v) => {
      toast.success(v.enabled ? "تم تفعيل استلام البوت للعروض" : "تم تعطيل استلام البوت للعروض");
      refresh();
    },
    onError: (e) => toast.error(e.message)
  });
  const toggleAll = useMutation({
    mutationFn: (enabled) => setAllFn({
      data: {
        enabled
      }
    }),
    onSuccess: (_d, enabled) => {
      toast.success(enabled ? "تم تفعيل الكل - البوت" : "تم تعطيل الكل - البوت");
      refresh();
    },
    onError: (e) => toast.error(e.message)
  });
  const busy = toggleOne.isPending || toggleAll.isPending;
  return /* @__PURE__ */ jsxs("section", { className: "mt-6 rounded-xl border border-slate-700 bg-slate-900 p-4 text-slate-100 shadow-lg", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-wrap items-center justify-between gap-3", children: [
      /* @__PURE__ */ jsx("h2", { className: "text-base md:text-lg font-bold", children: "تحكم البوت في استلام عروض الأسعار" }),
      /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ jsxs("button", { disabled: busy, onClick: () => toggleAll.mutate(true), className: "inline-flex items-center gap-1 rounded-md bg-emerald-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-emerald-500 disabled:opacity-60", children: [
          /* @__PURE__ */ jsx(Bot, { className: "h-4 w-4" }),
          " تفعيل الكل - البوت"
        ] }),
        /* @__PURE__ */ jsxs("button", { disabled: busy, onClick: () => toggleAll.mutate(false), className: "inline-flex items-center gap-1 rounded-md bg-red-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-red-500 disabled:opacity-60", children: [
          /* @__PURE__ */ jsx(BotOff, { className: "h-4 w-4" }),
          " تعطيل الكل - البوت"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsx("p", { className: "mt-1 text-xs text-slate-400", children: "هذا التحكم مستقل عن زر العميل: عند تعطيل مشروع يرفض البوت استلام أي عرض سعر له، وعند «تعطيل الكل» يرفض البوت كل العروض." }),
    isLoading ? /* @__PURE__ */ jsx("div", { className: "grid place-items-center py-8", children: /* @__PURE__ */ jsx(Loader2, { className: "h-5 w-5 animate-spin" }) }) : projects.length === 0 ? /* @__PURE__ */ jsx("div", { className: "py-6 text-center text-sm text-slate-400", children: "لا توجد مشاريع" }) : /* @__PURE__ */ jsx("ul", { className: "mt-3 divide-y divide-slate-800", children: projects.map((p) => /* @__PURE__ */ jsxs("li", { className: "flex items-center justify-between gap-3 py-2.5", children: [
      /* @__PURE__ */ jsx("span", { className: "text-sm font-medium", children: p.name }),
      /* @__PURE__ */ jsx("button", { type: "button", role: "switch", "aria-checked": p.bot_offers_enabled, "aria-label": `تشغيل أو إطفاء استلام البوت للعروض لمشروع ${p.name}`, disabled: busy, onClick: () => toggleOne.mutate({
        id: p.id,
        enabled: !p.bot_offers_enabled
      }), className: `relative inline-flex h-6 w-11 shrink-0 items-center rounded-full transition disabled:opacity-60 ${p.bot_offers_enabled ? "bg-emerald-600" : "bg-slate-600"}`, children: /* @__PURE__ */ jsx("span", { className: `absolute h-5 w-5 rounded-full bg-white transition-all ${p.bot_offers_enabled ? "start-[2px]" : "start-[22px]"}` }) })
    ] }, p.id)) })
  ] });
}
function SubmitterBadge({
  type
}) {
  const isUser = type === "user";
  return /* @__PURE__ */ jsx("span", { className: `mt-1 inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-semibold ${isUser ? "bg-emerald-500/15 text-emerald-300" : "bg-amber-500/15 text-amber-300"}`, children: isUser ? "👤 مستخدم" : "🔔 زائر" });
}
function MessageModal({
  target,
  onClose
}) {
  const send = useServerFn(sendRequestMessage);
  const [message, setMessage] = useState("");
  const [sending, setSending] = useState(false);
  async function submit(e) {
    e.preventDefault();
    if (!message.trim()) {
      toast.error("اكتب نص الرسالة");
      return;
    }
    setSending(true);
    try {
      await send({
        data: {
          to: target.email,
          message: `الشركة: ${target.company}

${message.trim()}`
        }
      });
      toast.success("تم إرسال الرسالة");
      onClose();
    } catch (err) {
      toast.error(err instanceof Error ? err.message : "تعذر إرسال الرسالة");
    } finally {
      setSending(false);
    }
  }
  return /* @__PURE__ */ jsx("div", { className: "fixed inset-0 z-50 grid place-items-center bg-black/60 p-4", dir: "rtl", onClick: onClose, children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-lg rounded-xl border border-slate-700 bg-slate-900 p-5 text-slate-100 shadow-xl", onClick: (e) => e.stopPropagation(), children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-3 flex items-start justify-between gap-3", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("h2", { className: "text-lg font-bold", children: "رسالة خاصة" }),
        /* @__PURE__ */ jsxs("p", { className: "mt-0.5 text-xs text-slate-400", children: [
          target.company,
          " — ",
          /* @__PURE__ */ jsx("span", { dir: "ltr", children: target.email })
        ] })
      ] }),
      /* @__PURE__ */ jsx("button", { "aria-label": "إغلاق", onClick: onClose, className: "rounded-md p-1 text-slate-400 hover:bg-slate-800 hover:text-slate-100", children: /* @__PURE__ */ jsx(X, { className: "h-5 w-5" }) })
    ] }),
    /* @__PURE__ */ jsxs("form", { onSubmit: submit, className: "space-y-3", children: [
      /* @__PURE__ */ jsx("textarea", { value: message, onChange: (e) => setMessage(e.target.value), rows: 6, maxLength: 3e3, placeholder: "اكتب رسالتك هنا...", className: "w-full resize-y rounded-lg border border-slate-700 bg-slate-800 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500" }),
      /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-2", children: [
        /* @__PURE__ */ jsx("button", { type: "button", onClick: onClose, className: "rounded-md border border-slate-700 px-4 py-2 text-sm hover:bg-slate-800", children: "إلغاء" }),
        /* @__PURE__ */ jsxs("button", { type: "submit", disabled: sending, className: "inline-flex items-center gap-2 rounded-md bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-500 disabled:opacity-60", children: [
          sending ? /* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }) : /* @__PURE__ */ jsx(Mail, { className: "h-4 w-4" }),
          " إرسال"
        ] })
      ] })
    ] })
  ] }) });
}
export {
  RequestsPage as component
};
