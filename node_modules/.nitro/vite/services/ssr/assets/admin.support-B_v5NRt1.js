import { jsxs, jsx, Fragment } from "react/jsx-runtime";
import { u as useServerFn, ap as adminListChats, aq as adminListChatMessages, ar as adminReplyChat, as as adminCloseChat, at as adminDeleteAllSupport } from "./router-CtQuP2fc.js";
import { useQueryClient, useQuery } from "@tanstack/react-query";
import { useState, useRef, useEffect } from "react";
import { Headphones, Trash2, CheckCircle2, Send } from "lucide-react";
import { toast } from "sonner";
import "@tanstack/react-router";
import "./createSsrRpc-DY9HpWEz.js";
import "./server-COznR7QB.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "./auth-middleware.server-B9hAjfqi.js";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
import "zod";
import "./vip.repo-CycBrLVA.js";
import "./saudi-cities-D2sGDQV3.js";
import "@aws-sdk/client-s3";
import "./r2-CJ2zxhhj.js";
import "aws4fetch";
import "@lovable.dev/webhooks-js";
import "@react-email/components";
import "@lovable.dev/email-js";
function AdminSupportPage() {
  const qc = useQueryClient();
  const listChats = useServerFn(adminListChats);
  const listMsgs = useServerFn(adminListChatMessages);
  const reply = useServerFn(adminReplyChat);
  const closeFn = useServerFn(adminCloseChat);
  const delAllFn = useServerFn(adminDeleteAllSupport);
  const [activeId, setActiveId] = useState(null);
  const [body, setBody] = useState("");
  const [sending, setSending] = useState(false);
  const scrollRef = useRef(null);
  const {
    data: chats = []
  } = useQuery({
    queryKey: ["admin-support-chats"],
    queryFn: () => listChats(),
    refetchInterval: 5e3
  });
  const {
    data: messages = []
  } = useQuery({
    queryKey: ["admin-support-msgs", activeId],
    queryFn: () => listMsgs({
      data: {
        chatId: activeId
      }
    }),
    enabled: !!activeId,
    refetchInterval: activeId ? 3e3 : false
  });
  useEffect(() => {
    scrollRef.current?.scrollTo({
      top: scrollRef.current.scrollHeight,
      behavior: "smooth"
    });
  }, [messages.length, activeId]);
  async function handleSend(e) {
    e?.preventDefault();
    if (!activeId || !body.trim() || sending) return;
    setSending(true);
    try {
      await reply({
        data: {
          chatId: activeId,
          body: body.trim()
        }
      });
      setBody("");
      qc.invalidateQueries({
        queryKey: ["admin-support-msgs", activeId]
      });
    } catch (err) {
      toast.error(err?.message ?? "تعذر الإرسال");
    } finally {
      setSending(false);
    }
  }
  async function handleClose() {
    if (!activeId) return;
    await closeFn({
      data: {
        chatId: activeId
      }
    });
    qc.invalidateQueries({
      queryKey: ["admin-support-chats"]
    });
    toast.success("تم إغلاق المحادثة");
  }
  return /* @__PURE__ */ jsxs("div", { className: "mx-auto max-w-6xl", children: [
    /* @__PURE__ */ jsxs("div", { className: "mb-4 flex items-center justify-between gap-2", children: [
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsx("span", { className: "grid h-9 w-9 place-items-center rounded-lg bg-[image:var(--gradient-accent)] text-accent-foreground", children: /* @__PURE__ */ jsx(Headphones, { className: "h-5 w-5" }) }),
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("h1", { className: "text-xl font-bold", children: "دعم العملاء" }),
          /* @__PURE__ */ jsx("p", { className: "text-xs text-muted-foreground", children: "كل محادثات العملاء والبوت" })
        ] })
      ] }),
      chats.length > 0 && /* @__PURE__ */ jsxs("button", { onClick: async () => {
        if (!confirm("حذف جميع محادثات ورسائل الدعم؟ لا يمكن التراجع.")) return;
        try {
          await delAllFn();
          setActiveId(null);
          qc.invalidateQueries({
            queryKey: ["admin-support-chats"]
          });
          qc.invalidateQueries({
            queryKey: ["admin-support-msgs"]
          });
          toast.success("تم حذف جميع الرسائل");
        } catch (err) {
          toast.error(err?.message ?? "تعذر الحذف");
        }
      }, className: "inline-flex items-center gap-1.5 rounded-md bg-red-600/20 px-3 py-1.5 text-xs font-medium text-red-300 hover:bg-red-600/30", children: [
        /* @__PURE__ */ jsx(Trash2, { className: "h-3.5 w-3.5" }),
        " حذف الكل"
      ] })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid h-[calc(100vh-220px)] min-h-[500px] grid-cols-1 gap-3 md:grid-cols-[280px_1fr]", children: [
      /* @__PURE__ */ jsx("div", { className: "overflow-y-auto rounded-xl border border-border bg-background", children: chats.length === 0 ? /* @__PURE__ */ jsx("p", { className: "p-4 text-center text-sm text-muted-foreground", children: "لا توجد محادثات" }) : chats.map((c) => /* @__PURE__ */ jsxs("button", { onClick: () => setActiveId(c.id), className: `block w-full border-b border-border px-3 py-2 text-right text-sm transition ${activeId === c.id ? "bg-secondary" : "hover:bg-secondary/50"}`, children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsx("span", { className: "font-medium", children: c.visitor_name || `زائر ${c.id.slice(0, 6)}` }),
          /* @__PURE__ */ jsx("span", { className: `rounded-full px-1.5 py-0.5 text-[10px] ${c.status === "escalated" ? "bg-accent text-accent-foreground" : c.status === "closed" ? "bg-muted text-muted-foreground" : "bg-secondary text-secondary-foreground"}`, children: c.status === "escalated" ? "بحاجة موظف" : c.status === "closed" ? "مغلق" : "بوت" })
        ] }),
        /* @__PURE__ */ jsx("div", { className: "mt-0.5 text-[11px] text-muted-foreground", children: new Date(c.last_message_at).toLocaleString("ar") })
      ] }, c.id)) }),
      /* @__PURE__ */ jsx("div", { className: "flex flex-col overflow-hidden rounded-xl border border-border bg-background", children: !activeId ? /* @__PURE__ */ jsx("div", { className: "grid flex-1 place-items-center text-sm text-muted-foreground", children: "اختر محادثة لعرضها" }) : /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between border-b border-border bg-secondary/40 px-3 py-2", children: [
          /* @__PURE__ */ jsx("div", { className: "text-sm font-semibold", children: "المحادثة" }),
          /* @__PURE__ */ jsxs("button", { onClick: handleClose, className: "inline-flex items-center gap-1 rounded-md border border-border bg-background px-2 py-1 text-xs hover:bg-secondary", children: [
            /* @__PURE__ */ jsx(CheckCircle2, { className: "h-3.5 w-3.5" }),
            " إغلاق"
          ] })
        ] }),
        /* @__PURE__ */ jsx("div", { ref: scrollRef, className: "flex-1 space-y-2 overflow-y-auto p-3", children: messages.map((m) => {
          if (m.sender === "system") {
            return /* @__PURE__ */ jsx("div", { className: "mx-auto max-w-[80%] rounded-md bg-accent/15 px-3 py-1.5 text-center text-[11px] text-foreground/70", children: m.body }, m.id);
          }
          const isAdminMsg = m.sender === "admin";
          return /* @__PURE__ */ jsx("div", { className: `flex ${isAdminMsg ? "justify-end" : "justify-start"}`, children: /* @__PURE__ */ jsxs("div", { className: `max-w-[80%] rounded-2xl px-3 py-2 text-sm shadow-sm ${isAdminMsg ? "bg-primary text-primary-foreground" : m.sender === "bot" ? "bg-secondary" : "bg-background border border-border"}`, children: [
            /* @__PURE__ */ jsxs("div", { className: "mb-0.5 text-[10px] font-semibold opacity-70", children: [
              m.sender === "admin" ? "موظف" : m.sender === "bot" ? "بوت" : "عميل",
              " · ",
              new Date(m.created_at).toLocaleString("ar")
            ] }),
            /* @__PURE__ */ jsx("div", { className: "whitespace-pre-wrap break-words", dangerouslySetInnerHTML: {
              __html: m.body
            } })
          ] }) }, m.id);
        }) }),
        /* @__PURE__ */ jsxs("form", { onSubmit: handleSend, className: "flex items-end gap-2 border-t border-border bg-secondary/30 p-3", children: [
          /* @__PURE__ */ jsx("textarea", { value: body, onChange: (e) => setBody(e.target.value), onKeyDown: (e) => {
            if (e.key === "Enter" && !e.shiftKey) {
              e.preventDefault();
              handleSend();
            }
          }, rows: 1, maxLength: 4e3, placeholder: "اكتب ردك…", className: "max-h-32 flex-1 resize-none rounded-md border border-border bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring" }),
          /* @__PURE__ */ jsxs("button", { type: "submit", disabled: sending || !body.trim(), className: "inline-flex h-10 items-center gap-1.5 rounded-md bg-foreground px-3 text-sm font-medium text-background disabled:opacity-50", children: [
            /* @__PURE__ */ jsx(Send, { className: "h-4 w-4" }),
            " إرسال"
          ] })
        ] })
      ] }) })
    ] })
  ] });
}
export {
  AdminSupportPage as component
};
