import { c as createServerRpc } from "./createServerRpc-DYLDSQ_Q.js";
import { c as createServerFn } from "./server-COznR7QB.js";
import { z } from "zod";
import { a as requireAuth } from "./auth-middleware.server-B9hAjfqi.js";
import { addSupportMessage, listMessages, getChatByVisitorToken, deleteVisitorChat, listSupportChats, updateChatStatus, getChatById, deleteAllSupport, countEscalatedChats, createVisitorChat } from "./support.repo-DmHG3-Jr.js";
import { listAllProjects, getProjectExclusive, getById } from "./projects.repo-DX_seSWS.js";
import { g as getBotSettingsRow } from "./bot-settings.repo-71vbkXe-.js";
import { c as cached, d as TTL_CHAT, a as cacheKeys, e as invalidateChat, f as invalidate } from "./cache-sw4uQcOM.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "react";
import "@tanstack/react-router";
import "react/jsx-runtime";
import "@tanstack/react-router/ssr/server";
import "./db-D5OYORU-.js";
import "bcryptjs";
import "jose";
import "@libsql/client";
const uuid = z.string().uuid();
const STAFF_KEYWORDS = ["موظف", "موظفة", "خدمة العملاء", "الدعم", "كلم موظف", "أريد موظف", "اريد موظف", "human", "agent", "support"];
function assertStaff(roles) {
  if (!roles.includes("admin") && !roles.includes("employee")) throw new Error("Forbidden");
}
function assertAdmin(roles) {
  if (!roles.includes("admin")) throw new Error("Forbidden");
}
function wantsHuman(text) {
  const t = (text ?? "").toLowerCase();
  return STAFF_KEYWORDS.some((k) => t.includes(k.toLowerCase()));
}
function matchQa(qas, text) {
  const t = text.trim().toLowerCase();
  if (!t) return null;
  for (const q of qas) {
    if (q.question.toLowerCase().includes(t) || t.includes(q.question.toLowerCase())) return q;
    for (const k of q.keywords ?? []) {
      const kk = (k ?? "").toLowerCase().trim();
      if (kk && t.includes(kk)) return q;
    }
  }
  return null;
}
const STATUS_MAP = {
  active: "مفتوح للعروض",
  delivered: "تم التسليم",
  cancelled: "ملغي"
};
const STATUS_BADGE = {
  active: `<span style="background:#ffc107;color:white;font-weight:bold;padding:10px 20px;border-radius:8px;display:inline-block">🟡 مفتوح للعروض</span>`,
  delivered: `<span style="background:#28a745;color:white;font-weight:bold;padding:10px 20px;border-radius:8px;display:inline-block">✅ تم التسليم</span>`,
  cancelled: `<span style="background:#dc3545;color:white;font-weight:bold;padding:10px 20px;border-radius:8px;display:inline-block">❌ ملغي</span>`
};
const STOP_WORDS = /* @__PURE__ */ new Set(["مشروع", "المشروع", "مشاريع", "المشاريع", "project", "projects", "حالة", "حاله", "وضع", "status", "معلومات", "تفاصيل", "عن", "بخصوص", "ايش", "ما", "هو", "هي", "كم", "عدد", "count", "how", "many", "في", "من", "الى", "على", "the", "a", "an", "is", "what", "tell", "me", "about", "لو", "سمحت", "ممكن", "please"]);
function normalizeAr(s) {
  return (s ?? "").toLowerCase().replace(/[؟.!،,:;()"'`]/g, " ").replace(/[أإآ]/g, "ا").replace(/ى/g, "ي").replace(/ة/g, "ه").replace(/\s+/g, " ").trim();
}
function tokens(s) {
  return normalizeAr(s).split(" ").filter((w) => w && !STOP_WORDS.has(w));
}
function projectDetails(p) {
  const lines = [`📌 ${p.name}`, `الحالة: ${STATUS_BADGE[p.status] ?? p.status}`, `الموقع: ${p.location ?? "-"}`];
  if (p.duration) lines.push(`المدة: ${p.duration}`);
  if (p.description) lines.push(`الوصف: ${p.description.slice(0, 300)}`);
  return lines.join("\n");
}
function findProjectByQuery(rows, query) {
  const qTokens = tokens(query);
  if (!qTokens.length) return -1;
  let bestIdx = -1;
  let bestScore = 0;
  rows.forEach((r, i) => {
    const nameNorm = normalizeAr(r.name);
    const locNorm = normalizeAr(r.location ?? "");
    let score = 0;
    const qJoined = qTokens.join(" ");
    if (nameNorm && (nameNorm === qJoined || nameNorm.includes(qJoined) || qJoined.includes(nameNorm))) score += 10;
    for (const t of qTokens) {
      if (t.length < 2) continue;
      if (nameNorm.includes(t)) score += 3;
      else if (locNorm && locNorm.includes(t)) score += 1;
    }
    if (score > bestScore) {
      bestScore = score;
      bestIdx = i;
    }
  });
  return bestScore >= 3 ? bestIdx : -1;
}
async function answerProjectQuery(text) {
  const raw = (text ?? "").trim();
  if (!raw) return null;
  const tNorm = normalizeAr(raw);
  const projectKeywords = ["مشروع", "مشاريع", "project"];
  const hasProjectWord = projectKeywords.some((k) => tNorm.includes(normalizeAr(k)));
  const rows = (await listAllProjects()).filter((p) => p.admin_approval === "approved");
  if (!rows.length) return hasProjectWord ? "لا توجد مشاريع متاحة حالياً." : null;
  if (hasProjectWord && (tNorm.includes("كم") || tNorm.includes("عدد") || tNorm.includes("count") || tNorm.includes("how many"))) {
    const active = rows.filter((r) => r.status === "active").length;
    const delivered = rows.filter((r) => r.status === "delivered").length;
    return `عدد المشاريع المعتمدة: ${rows.length}
• مفتوح للعروض: ${active}
• تم التسليم: ${delivered}`;
  }
  const cityRe = /^\s*(?:مشاريع|projects)\s+(?:في|by|in)?\s*(.+)$/i;
  const cm = raw.match(cityRe);
  if (cm && cm[1]) {
    const cityRaw = cm[1].trim();
    const city = normalizeAr(cityRaw);
    if (city && !["المعتمده", "المتاحه", "المفتوحه", "كلها", "الكل"].includes(city)) {
      const matches = rows.filter((r) => {
        const c = normalizeAr((r.city ?? r.location ?? "").toString());
        return c && (c.includes(city) || city.includes(c));
      });
      if (matches.length) {
        return `مشاريع ${cityRaw}:

` + matches.slice(0, 20).map((p) => `• ${p.name} — ${STATUS_MAP[p.status] ?? p.status}`).join("\n");
      }
    }
  }
  const idx = findProjectByQuery(rows, raw);
  if (idx >= 0) {
    return projectDetails(rows[idx]);
  }
  if (!hasProjectWord) return null;
  let filtered = rows;
  if (tNorm.includes("مفتوح") || tNorm.includes("متاح")) filtered = rows.filter((p) => p.status === "active");
  else if (tNorm.includes("مسلم") || tNorm.includes("تسليم") || tNorm.includes("منجز")) filtered = rows.filter((p) => p.status === "delivered");
  else if (tNorm.includes("ملغ")) filtered = rows.filter((p) => p.status === "cancelled");
  if (!filtered.length) return "لا توجد مشاريع مطابقة لطلبك.";
  return "المشاريع المتاحة:\n\n" + filtered.slice(0, 20).map((p) => `• ${p.name} — ${p.location ?? "-"} — ${STATUS_MAP[p.status] ?? p.status}`).join("\n");
}
const ASK_REQUEST_PROMPT = "للاستعلام عن حالة طلبكم، أرسل البريد الإلكتروني أو اسم الشركة المستخدم في الطلب 🙏";
const REQUEST_NOT_FOUND = "لم يتم العثور على طلب";
const OFFER_PENDING_REPLY = "تم ارسال طلبكم وبانتظار موافقة الادارة";
const REQUEST_STATUS_REPLY = {
  new: "🆕 تم استلام طلبكم وشكرا لثقتكم بنا ✅",
  reviewing: "🆕 طلبكم قيد المراجعة حالياً",
  accepted: "🟠 تم قبول طلبكم 🎉",
  rejected: "❌ نأسف تم رفض طلبكم. يمكنكم مراسلتنا عبر بوابة تواصل بنا لمعرفة التفاصيل 🙏"
};
const REQUEST_KEYWORDS = ["طلبي", "طلبنا", "حالة طلب", "حالة الطلب", "استعلام عن طلب", "وين طلبي", "وش صار على طلبي", "متابعة طلب", "request status", "my request"];
function asksAboutRequest(text) {
  const t = normalizeAr(text);
  return REQUEST_KEYWORDS.some((k) => t.includes(normalizeAr(k)));
}
const EMAIL_RE = /[\w.+-]+@[\w-]+\.[\w.-]+/;
async function answerRequestStatus(query) {
  const raw = (query ?? "").trim();
  if (!raw) return null;
  const repo = await import("./project-requests.repo-BJrIn2i8.js");
  const notificationsRepo = await import("./notifications.repo-vog42ua4.js");
  const emailMatch = raw.match(EMAIL_RE);
  const name = raw.replace(/(حالة|طلب|طلبي|الطلب|شركة|شركه)/g, " ").replace(/\s+/g, " ").trim() || raw;
  let rows = emailMatch ? await repo.searchRequestsByEmail(emailMatch[0]) : [];
  if (!rows.length && !emailMatch) rows = await repo.searchRequestsByCompany(name);
  if (rows.length) {
    const rowsToShow = rows.slice(0, 5);
    const projectNames = /* @__PURE__ */ new Map();
    await Promise.all(rowsToShow.filter((r) => r.project_id).map(async (r) => {
      if (!projectNames.has(r.project_id)) {
        const p = await getById(r.project_id).catch(() => null);
        projectNames.set(r.project_id, p?.name ?? "—");
      }
    }));
    return rowsToShow.map((r) => {
      const label = r.status === "new" ? `<span style="background:#17a2b8;color:white;font-weight:bold;padding:8px 16px;border-radius:20px;display:inline-block;font-size:14px">📥 جديد</span>` : r.status === "reviewing" ? `<span style="background:#fd7e14;color:white;font-weight:bold;padding:8px 16px;border-radius:20px;display:inline-block;font-size:14px">⏳ قيد المراجعة</span>` : r.status === "accepted" ? `<span style="background:#28a745;color:white;font-weight:bold;padding:8px 16px;border-radius:50px;display:inline-block;font-size:14px">● ✅ مقبول</span>` : r.status === "rejected" ? `<span style="background:#dc3545;color:white;font-weight:bold;padding:12px 24px;border-radius:8px;display:inline-block;font-size:16px">❌ مرفوض</span>` : r.status;
      const projName = r.project_id ? projectNames.get(r.project_id) ?? "—" : "—";
      const lines = [`📄 ${r.company_name ?? "طلب"}`, `المشروع: ${projName}`, `حالة الطلب: ${label}`];
      if (r.note && r.note.trim()) lines.push(`الملاحظة: ${r.note.trim()}`);
      else lines.push(REQUEST_STATUS_REPLY[r.status] ?? REQUEST_STATUS_REPLY.new);
      return lines.join("\n");
    }).join("\n\n");
  }
  let offers = emailMatch ? await notificationsRepo.searchOfferNotificationsByEmail(emailMatch[0]) : [];
  if (!offers.length && !emailMatch) offers = await notificationsRepo.searchOfferNotificationsByCompany(name);
  if (offers.length) return OFFER_PENDING_REPLY;
  return REQUEST_NOT_FOUND;
}
const OFFER_FLOW_MARKER = "__OFFER_FLOW__";
const OFFER_KEYWORDS = ["كيف اقدم عرض سعر", "كيف أقدم عرض سعر", "عرض سعر", "تقديم عرض", "اقدم عرض", "أقدم عرض", "ارفع عرض", "أرفع عرض", "تسعير", "quote", "price offer", "submit offer"];
const OFFER_TERMS = ["لتقديم عرض سعر، وافق على الشروط أولاً:", "", "1) العرض يجب أن يكون بصيغة PDF واضحة ومختومة.", "2) السعر المقدم نهائي وساري لمدة 30 يوماً على الأقل.", "3) الالتزام بمدة تنفيذ المشروع المعلنة في تفاصيل المشروع.", "4) صحة البيانات (اسم الشركة والبريد الإلكتروني) مسؤولية مقدّم العرض.", "5) تقديم العرض لا يعني قبوله، وسيتم إشعاركم بأي تحديث."].join("\n");
function asksAboutOffer(text) {
  const t = normalizeAr(text);
  return OFFER_KEYWORDS.some((k) => t.includes(normalizeAr(k)));
}
const VIP_FLOW_MARKER = "__VIP_FLOW__";
const VIP_KEYWORDS = ["اشترك", "اشتراك", "vip", "ابغى اشترك", "اريد اشتراك", "اريد اشترك", "ابغى اشتراك", "الاشتراك", "اشتراك vip", "باقة", "باقه", "العملاء المميزون", "مميز", "اشتراك مميز", "اشترك vip"];
const VIP_PLANS_TEXT = ["باقات اشتراك VIP المتاحة:", "", "باقة 100 ريال — 30 يوم", "تستقبل مشاريع خاصة عبر الإيميل بلا منافس + دعم فني VIP", "", "باقة 200 ريال — 60 يوم", "تستقبل مشاريع خاصة عبر الإيميل بلا منافس + دعم فني VIP", "", "باقة 300 ريال — 90 يوم", "تستقبل مشاريع خاصة عبر الإيميل بلا منافس + دعم فني VIP", "", "بيانات التحويل البنكي:", "البنك الأهلي — IBAN: SA35 1000 0065 5000 4711 0807", "", "اختر الباقة التي تناسبك، ثم عبّئ بياناتك وارفع صورة الإيصال."].join("\n");
function asksAboutVip(text) {
  const t = normalizeAr(text);
  return VIP_KEYWORDS.some((k) => t.includes(normalizeAr(k)));
}
async function askGroq(userText, opts) {
  const apiKey = process.env.GROQ_API_KEY;
  if (!apiKey) return null;
  const model = process.env.GROQ_MODEL || "llama-3.3-70b-versatile";
  const ENFORCE_AR = ["### تعليمات حديدية لا يجوز مخالفتها أبداً ###", "1. ردك يجب أن يكون باللغة العربية ONLY. ممنوع تماماً استخدام أي كلمة إنجليزية ما لم يكن مصطلحاً تقنياً لا يترجم.", "2. اللهجة: سعودية ودودة 100%. ممنوع الفصحى المعقدة وممنوع الإنجليزية نهائياً.", '3. اختصار: ردك 3 أسطر بالكثير. لا تبدأ بـ "كمساعد ذكي" ولا تعتذر.', "4. لا تكتب think ولا --- ولا تشرح لنفسك. رد النتيجة فقط.", '5. إذا كان السؤال خارج نطاق المنصة قل: "انا مساعد العمران بس 😊 اقدر اخدمك في شي يخص المنصة؟"', '6. الدفع والعقود والحسابات: ردك ثابت: "لمساعدتك بشكل دقيق يرجى التواصل مع الدعم الفني".', '7. لا تأليف أبداً. إذا ما تدري قول: "للتفاصيل الدقيقة راجع قسم المشاريع او كلم الدعم الفني".', "8. نحن منصة إلكترونية 100%. كل الخدمات أونلاين. ما عندنا مقر."].join("\n");
  const sysParts = [ENFORCE_AR, opts.systemInstruction?.trim(), opts.botName ? `اسمك: ${opts.botName}.` : null, opts.dialect ? `اللهجة المطلوبة: ${opts.dialect}.` : null, opts.scope ? `نطاق عملك: ${opts.scope}` : null, "### تذكير أخير: رد بالعربية السعودية فقط. أي رد بغير العربية مرفوض. ###"].filter(Boolean);
  try {
    const res = await fetch("https://api.groq.com/openai/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${apiKey}`
      },
      body: JSON.stringify({
        model,
        temperature: 0.2,
        max_tokens: 400,
        messages: [...sysParts.length ? [{
          role: "system",
          content: sysParts.join("\n")
        }] : [], {
          role: "user",
          content: userText
        }]
      })
    });
    if (!res.ok) return null;
    const j = await res.json();
    const text = j?.choices?.[0]?.message?.content?.trim();
    if (!text) return null;
    for (const bad of opts.blockedReplies ?? []) {
      if (bad && text.toLowerCase().includes(bad.toLowerCase())) return null;
    }
    const englishRatio = (text.match(/[a-zA-Z]{2,}/g) ?? []).length / Math.max(text.split(/\s+/).length, 1);
    if (englishRatio > 0.4) return null;
    return text;
  } catch {
    return null;
  }
}
const DAY_KEYS = ["sun", "mon", "tue", "wed", "thu", "fri", "sat"];
function isInWorkHours(settings) {
  if (!settings.work_days || !settings.work_start || !settings.work_end) return true;
  const now = /* @__PURE__ */ new Date();
  const utcMinutes = now.getUTCHours() * 60 + now.getUTCMinutes();
  const localMinutes = (utcMinutes + 3 * 60) % (24 * 60);
  const dayIdx = (now.getUTCDay() + Math.floor((utcMinutes + 3 * 60) / (24 * 60))) % 7;
  const dayKey = DAY_KEYS[dayIdx];
  if (!settings.work_days[dayKey]) return false;
  const [sh, sm] = settings.work_start.split(":").map(Number);
  const [eh, em] = settings.work_end.split(":").map(Number);
  const start = sh * 60 + sm;
  const end = eh * 60 + em;
  return localMinutes >= start && localMinutes <= end;
}
async function getOrCreateVisitorChat(visitorToken, visitorName) {
  const existing = await getChatByVisitorToken(visitorToken);
  if (existing) return existing;
  const created = await createVisitorChat(visitorToken);
  await addSupportMessage(created.id, "bot", "أهلًا بك في دعم العمران! اختر سؤالًا من الأسفل أو اطلب التحدث مع موظف.");
  return created;
}
const ALERT_MARKER = "__ALERT_SENT__";
const BUSY_REPLY = "الموظفين مشغولين حالياً. كيف أقدر أساعدك؟";
async function sendWaitingAlert(chatId, visitorName) {
  const to = process.env.VITE_ALERT_EMAIL || process.env.ALERT_EMAIL;
  const key = process.env.VITE_RESEND_API_KEY || process.env.RESEND_API_KEY;
  if (!to || !key) {
    console.error("alert email/key missing");
    return;
  }
  try {
    const res = await fetch("https://api.resend.com/emails", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${key}`
      },
      body: JSON.stringify({
        from: "Alamran <send@ali-alhaddad.com>",
        to: [to],
        subject: "🚨 عميل ينتظر",
        html: `<p><strong>الاسم:</strong> ${visitorName ?? "زائر"}</p><p><strong>customer_id:</strong> ${chatId}</p>`
      })
    });
    if (!res.ok) console.error("waiting alert failed", res.status, await res.text());
  } catch (e) {
    console.error("waiting alert exception", e);
  }
}
async function agentRepliedSince(chatId, sinceIso) {
  const msgs = await listMessages(chatId, sinceIso);
  return msgs.some((m) => m.sender === "admin");
}
async function recentAlertExists(chatId) {
  const {
    db,
    rowsToObjects
  } = await import("./db-D5OYORU-.js").then((n) => n.e);
  const cutoff = new Date(Date.now() - 10 * 60 * 1e3).toISOString();
  const r = await db.execute(`SELECT id FROM support_messages WHERE chat_id = ? AND sender = 'system' AND body = ? AND created_at > ? LIMIT 1`, [chatId, ALERT_MARKER, cutoff]);
  return rowsToObjects(r).length > 0;
}
function scheduleEscalationWatchers(chatId, visitorName, startIso) {
  setTimeout(async () => {
    try {
      if (await agentRepliedSince(chatId, startIso)) return;
      if (await recentAlertExists(chatId)) return;
      await sendWaitingAlert(chatId, visitorName);
      await addSupportMessage(chatId, "system", ALERT_MARKER);
    } catch (e) {
      console.error("watcher-30s", e);
    }
  }, 3e4);
  setTimeout(async () => {
    try {
      if (await agentRepliedSince(chatId, startIso)) return;
      await addSupportMessage(chatId, "bot", BUSY_REPLY);
    } catch (e) {
      console.error("watcher-60s", e);
    }
  }, 6e4);
}
async function escalateOrOffHours(chatId) {
  const settings = await getBotSettingsRow();
  const offHours = settings ? !isInWorkHours(settings) : false;
  if (offHours || settings?.allow_escalation === false) {
    await addSupportMessage(chatId, "bot", settings?.off_hours_message?.trim() || "نحن خارج ساعات العمل حالياً. سنرد عليك في أقرب وقت.");
    return {
      escalated: false
    };
  }
  await updateChatStatus(chatId, "escalated");
  await addSupportMessage(chatId, "system", "تم تحويل محادثتك لموظف الدعم. سيتم الرد عليك في أقرب وقت.");
  const chat = await getChatById(chatId);
  scheduleEscalationWatchers(chatId, chat?.visitor_name ?? null, (/* @__PURE__ */ new Date()).toISOString());
  return {
    escalated: true
  };
}
const listBotQuestions_createServerFn_handler = createServerRpc({
  id: "473edaaa49ffe54bc8f383fbdac7511392187f10acbab0c8c7449c1b7764ac54",
  name: "listBotQuestions",
  filename: "src/lib/support.functions.ts"
}, (opts) => listBotQuestions.__executeServer(opts));
const listBotQuestions = createServerFn({
  method: "GET"
}).handler(listBotQuestions_createServerFn_handler, async () => {
  const {
    listActiveForVisitors
  } = await import("./bot-qa.repo-CtrPP0pM.js");
  return listActiveForVisitors();
});
const startVisitorChat_createServerFn_handler = createServerRpc({
  id: "d2395d71d5cf32fad3b7d198197abb2c2cf523dd2de7ce34028e5102040cfee9",
  name: "startVisitorChat",
  filename: "src/lib/support.functions.ts"
}, (opts) => startVisitorChat.__executeServer(opts));
const startVisitorChat = createServerFn({
  method: "POST"
}).inputValidator((d) => z.object({
  visitorToken: uuid,
  visitorName: z.string().trim().max(80).nullable().optional()
}).parse(d)).handler(startVisitorChat_createServerFn_handler, async ({
  data
}) => getOrCreateVisitorChat(data.visitorToken, data.visitorName));
const visitorGetMessages_createServerFn_handler = createServerRpc({
  id: "1b6c0c9c1c9f08324d604e475dc4c3c8a4b09a16f89c8dc654c7f5866c6474af",
  name: "visitorGetMessages",
  filename: "src/lib/support.functions.ts"
}, (opts) => visitorGetMessages.__executeServer(opts));
const visitorGetMessages = createServerFn({
  method: "POST"
}).inputValidator((d) => z.object({
  visitorToken: uuid,
  sinceIso: z.string().nullable().optional()
}).parse(d)).handler(visitorGetMessages_createServerFn_handler, async ({
  data
}) => {
  const load = async () => {
    const chat = await getChatByVisitorToken(data.visitorToken);
    if (!chat) return {
      chat: null,
      messages: []
    };
    return {
      chat,
      messages: await listMessages(chat.id, data.sinceIso)
    };
  };
  if (data.sinceIso) return load();
  return cached(cacheKeys.chat(data.visitorToken), TTL_CHAT, load);
});
const visitorSendMessage_createServerFn_handler = createServerRpc({
  id: "8d3baad434375fac495bca852705012d384f30fc6b3bb22190dd852beb6f28d8",
  name: "visitorSendMessage",
  filename: "src/lib/support.functions.ts"
}, (opts) => visitorSendMessage.__executeServer(opts));
const visitorSendMessage = createServerFn({
  method: "POST"
}).inputValidator((d) => z.object({
  visitorToken: uuid,
  body: z.string().trim().min(1).max(2e3),
  qaId: z.preprocess((v) => v == null || v === "" ? null : String(v), z.string().nullable()).optional()
}).parse(d)).handler(visitorSendMessage_createServerFn_handler, async ({
  data
}) => {
  await invalidateChat(data.visitorToken);
  const chat = await getOrCreateVisitorChat(data.visitorToken);
  await addSupportMessage(chat.id, "visitor", data.body);
  if (chat.status !== "bot") {
    await invalidateChat(data.visitorToken);
    return {
      ok: true
    };
  }
  const settings = await getBotSettingsRow();
  const botQa = await import("./bot-qa.repo-CtrPP0pM.js");
  let triggerEscalate = wantsHuman(data.body);
  let answer = null;
  if (data.qaId) {
    const qa = await botQa.getQaById(data.qaId);
    triggerEscalate = qa?.action === "escalate";
    answer = qa?.answer ?? null;
  } else if (!triggerEscalate && settings?.local_enabled !== false) {
    const m = matchQa(await botQa.listActiveQa(), data.body);
    triggerEscalate = m?.action === "escalate";
    answer = m?.answer ?? null;
  }
  if (triggerEscalate) {
    await escalateOrOffHours(chat.id);
    await invalidateChat(data.visitorToken);
    return {
      ok: true
    };
  }
  if (!answer && asksAboutOffer(data.body)) {
    const allRows = (await listAllProjects()).filter((p) => p.admin_approval === "approved");
    const pIdx = findProjectByQuery(allRows, data.body);
    if (pIdx >= 0) {
      const excl = await getProjectExclusive(allRows[pIdx].id);
      if (excl && Date.now() < new Date(excl.vip_end_at).getTime()) {
        const remainingMs = new Date(excl.vip_end_at).getTime() - Date.now();
        const hrs = Math.floor(remainingMs / 36e5);
        const mins = Math.floor(remainingMs % 36e5 / 6e4);
        await addSupportMessage(chat.id, "bot", `هذا المشروع في فترة حصرية. المتبقي: ${hrs} ساعة و ${mins} دقيقة`);
        await invalidateChat(data.visitorToken);
        return {
          ok: true
        };
      }
    }
    await addSupportMessage(chat.id, "bot", `${OFFER_TERMS}
${OFFER_FLOW_MARKER}`);
    await invalidateChat(data.visitorToken);
    return {
      ok: true
    };
  }
  if (!answer && asksAboutVip(data.body)) {
    await addSupportMessage(chat.id, "bot", `${VIP_PLANS_TEXT}
${VIP_FLOW_MARKER}`);
    await invalidateChat(data.visitorToken);
    return {
      ok: true
    };
  }
  let requestAnswer = null;
  if (!answer) {
    const prev = await listMessages(chat.id);
    const lastBot = [...prev].reverse().find((m) => m.sender === "bot");
    const awaitingData = lastBot?.body?.trim() === ASK_REQUEST_PROMPT;
    if (awaitingData) {
      requestAnswer = await answerRequestStatus(data.body);
    } else if (asksAboutRequest(data.body)) {
      requestAnswer = EMAIL_RE.test(data.body) || data.body.trim().split(/\s+/).length > 2 ? await answerRequestStatus(data.body) ?? ASK_REQUEST_PROMPT : ASK_REQUEST_PROMPT;
      if (requestAnswer === REQUEST_NOT_FOUND && !EMAIL_RE.test(data.body)) requestAnswer = ASK_REQUEST_PROMPT;
    }
  }
  const projectAnswer = requestAnswer ? null : await answerProjectQuery(data.body);
  let finalAnswer = answer || requestAnswer || projectAnswer;
  if (!finalAnswer && settings?.groq_enabled !== false) {
    finalAnswer = await askGroq(data.body, {
      systemInstruction: settings?.gemini_system_instruction,
      dialect: settings?.gemini_dialect,
      botName: settings?.gemini_bot_name,
      scope: settings?.gemini_scope,
      blockedReplies: settings?.gemini_blocked_replies
    });
  }
  answer = finalAnswer || settings?.fallback_message?.trim() || 'عذرًا، لا أملك إجابة على هذا السؤال. يمكنك كتابة "موظف" للتحدث مع الدعم.';
  await addSupportMessage(chat.id, "bot", answer);
  await invalidateChat(data.visitorToken);
  return {
    ok: true
  };
});
const visitorEscalate_createServerFn_handler = createServerRpc({
  id: "d4c835351eb03e09ca123caa1c91f29dde6054eb1fec80c344ad1b27997c3fd0",
  name: "visitorEscalate",
  filename: "src/lib/support.functions.ts"
}, (opts) => visitorEscalate.__executeServer(opts));
const visitorEscalate = createServerFn({
  method: "POST"
}).inputValidator((d) => z.object({
  visitorToken: uuid
}).parse(d)).handler(visitorEscalate_createServerFn_handler, async ({
  data
}) => {
  const chat = await getChatByVisitorToken(data.visitorToken);
  if (!chat) throw new Error("جلسة الشات غير موجودة");
  const res = await escalateOrOffHours(chat.id);
  await invalidateChat(data.visitorToken);
  return {
    ok: true,
    ...res
  };
});
const visitorEndSession_createServerFn_handler = createServerRpc({
  id: "ed6396123983567371e5efb71c9f60edd285e6d62bfab3a8c0a639e124ddbb8a",
  name: "visitorEndSession",
  filename: "src/lib/support.functions.ts"
}, (opts) => visitorEndSession.__executeServer(opts));
const visitorEndSession = createServerFn({
  method: "POST"
}).inputValidator((d) => z.object({
  visitorToken: uuid
}).parse(d)).handler(visitorEndSession_createServerFn_handler, async ({
  data
}) => {
  await deleteVisitorChat(data.visitorToken);
  await invalidateChat(data.visitorToken);
  return {
    ok: true
  };
});
const adminListChats_createServerFn_handler = createServerRpc({
  id: "e618c2143fa787d56d489687e0cce239f4041c5160149c846c9759048f21a75d",
  name: "adminListChats",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminListChats.__executeServer(opts));
const adminListChats = createServerFn({
  method: "GET"
}).middleware([requireAuth]).handler(adminListChats_createServerFn_handler, async ({
  context
}) => {
  assertStaff(context.roles);
  return listSupportChats();
});
const adminListChatMessages_createServerFn_handler = createServerRpc({
  id: "270c1762d98a54703b818e3db3c9d3ff72890dcaf8162eb203025f6a4651b70a",
  name: "adminListChatMessages",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminListChatMessages.__executeServer(opts));
const adminListChatMessages = createServerFn({
  method: "POST"
}).middleware([requireAuth]).inputValidator((d) => z.object({
  chatId: uuid
}).parse(d)).handler(adminListChatMessages_createServerFn_handler, async ({
  data,
  context
}) => {
  assertStaff(context.roles);
  return listMessages(data.chatId);
});
const adminReplyChat_createServerFn_handler = createServerRpc({
  id: "29185ffc54de4a4d32349e930389d6f85ee789e2c01a8d6a3f84c022fa6f57e1",
  name: "adminReplyChat",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminReplyChat.__executeServer(opts));
const adminReplyChat = createServerFn({
  method: "POST"
}).middleware([requireAuth]).inputValidator((d) => z.object({
  chatId: uuid,
  body: z.string().trim().min(1).max(4e3)
}).parse(d)).handler(adminReplyChat_createServerFn_handler, async ({
  data,
  context
}) => {
  assertStaff(context.roles);
  await addSupportMessage(data.chatId, "admin", data.body);
  await updateChatStatus(data.chatId, "escalated");
  const chat = await getChatById(data.chatId);
  if (chat?.visitor_token) await invalidateChat(chat.visitor_token);
  return {
    ok: true
  };
});
const adminCloseChat_createServerFn_handler = createServerRpc({
  id: "d737e3c35142fbf7cef6d3e25b70815a83a31ae693e8752175bca7ea3eaa9752",
  name: "adminCloseChat",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminCloseChat.__executeServer(opts));
const adminCloseChat = createServerFn({
  method: "POST"
}).middleware([requireAuth]).inputValidator((d) => z.object({
  chatId: uuid
}).parse(d)).handler(adminCloseChat_createServerFn_handler, async ({
  data,
  context
}) => {
  assertStaff(context.roles);
  await updateChatStatus(data.chatId, "closed");
  const chat = await getChatById(data.chatId);
  if (chat?.visitor_token) await invalidateChat(chat.visitor_token);
  return {
    ok: true
  };
});
const adminDeleteAllSupport_createServerFn_handler = createServerRpc({
  id: "cc7b4410257e035601dc7f884ef7ba3df2cbacbc439903a1805806314de9bcc5",
  name: "adminDeleteAllSupport",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminDeleteAllSupport.__executeServer(opts));
const adminDeleteAllSupport = createServerFn({
  method: "POST"
}).middleware([requireAuth]).handler(adminDeleteAllSupport_createServerFn_handler, async ({
  context
}) => {
  assertAdmin(context.roles);
  const chats = await listSupportChats();
  await deleteAllSupport();
  await invalidate(...chats.map((c) => c.visitor_token ? cacheKeys.chat(c.visitor_token) : null));
  return {
    ok: true
  };
});
const adminListBotQa_createServerFn_handler = createServerRpc({
  id: "29350f80d29a69d2e2195c7c03f4454ef10774b58808aa0149e6464d567e61d1",
  name: "adminListBotQa",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminListBotQa.__executeServer(opts));
const adminListBotQa = createServerFn({
  method: "GET"
}).middleware([requireAuth]).handler(adminListBotQa_createServerFn_handler, async ({
  context
}) => {
  assertAdmin(context.roles);
  const {
    listAllQa
  } = await import("./bot-qa.repo-CtrPP0pM.js");
  return listAllQa();
});
const adminUpsertBotQa_createServerFn_handler = createServerRpc({
  id: "8af3a0596161f6271665a3fe29acde3400e514840352ee5348d84d4a02310fad",
  name: "adminUpsertBotQa",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminUpsertBotQa.__executeServer(opts));
const adminUpsertBotQa = createServerFn({
  method: "POST"
}).middleware([requireAuth]).inputValidator((d) => z.object({
  id: z.string().uuid().nullable().optional(),
  question: z.string().trim().min(1).max(300),
  answer: z.string().trim().min(1).max(4e3),
  keywords: z.array(z.string().trim().max(60)).max(30),
  is_active: z.boolean(),
  sort_order: z.number().int().min(0).max(9999),
  action: z.enum(["none", "escalate"]).default("none")
}).parse(d)).handler(adminUpsertBotQa_createServerFn_handler, async ({
  data,
  context
}) => {
  assertAdmin(context.roles);
  const {
    upsertQa
  } = await import("./bot-qa.repo-CtrPP0pM.js");
  await upsertQa({
    ...data,
    id: data.id ?? null,
    action: data.action ?? "none"
  });
  return {
    ok: true
  };
});
const adminDeleteBotQa_createServerFn_handler = createServerRpc({
  id: "86797decd1cf50ed025b4a3c4c789fa623ea93fd3d645a56f1779a1cc6d3c125",
  name: "adminDeleteBotQa",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminDeleteBotQa.__executeServer(opts));
const adminDeleteBotQa = createServerFn({
  method: "POST"
}).middleware([requireAuth]).inputValidator((d) => z.object({
  id: uuid
}).parse(d)).handler(adminDeleteBotQa_createServerFn_handler, async ({
  data,
  context
}) => {
  assertAdmin(context.roles);
  const {
    deleteQa
  } = await import("./bot-qa.repo-CtrPP0pM.js");
  await deleteQa(data.id);
  return {
    ok: true
  };
});
const adminCountOpenSupportChats_createServerFn_handler = createServerRpc({
  id: "5dd5cca12724248959e8a8504ab6d9a9d61c11c357ec9dedaf9b423a674b79a2",
  name: "adminCountOpenSupportChats",
  filename: "src/lib/support.functions.ts"
}, (opts) => adminCountOpenSupportChats.__executeServer(opts));
const adminCountOpenSupportChats = createServerFn({
  method: "GET"
}).middleware([requireAuth]).handler(adminCountOpenSupportChats_createServerFn_handler, async ({
  context
}) => {
  assertStaff(context.roles);
  return {
    count: await countEscalatedChats()
  };
});
const VISION_MODELS = ["qwen/qwen3.6-27b"];
const RECEIPT_VISION_PROMPT = `You are an expert OCR assistant specialized in reading Saudi bank transfer receipts and payment app screenshots (Al Rajhi, AlAhli, STC Pay, Urpay, Apple Pay, mada, etc).
Read only the transfer amount and transaction date needed to validate this receipt. Search the entire image carefully, including small text and the receipt header/footer.
Respond with a JSON object only — no markdown, no explanation, no code fences:
{"amount":100,"date":"YYYY-MM-DD"}
Amount must be numeric only with no currency symbol or commas. The date may appear as DD/MM/YYYY, DD-MM-YYYY, YYYY/MM/DD, Arabic-Indic digits, or a Hijri date. Convert a Hijri date to Gregorian YYYY-MM-DD. If a date is visible, never return null; return the date you can read. Only return null when the field is genuinely not visible. Do not include thinking or reasoning text outside the JSON.`;
const RECEIPT_FOCUSED_PROMPT = `Read this Saudi bank transfer receipt. Find ONLY the transfer amount and transaction date. Search all small text carefully. Return JSON only: {"amount":100,"date":"the date exactly as visible"}. The date can be DD/MM/YYYY, DD-MM-YYYY, YYYY/MM/DD, Arabic-Indic digits, or Hijri. Do not return a null date if any date is visible.`;
function normalizeDigits(value) {
  return value.replace(/[٠-٩]/g, (digit) => String("٠١٢٣٤٥٦٧٨٩".indexOf(digit)));
}
function normalizeReceiptDate(value) {
  if (!value?.trim()) return null;
  const raw = normalizeDigits(value.trim()).replace(/[.]/g, "/");
  if (["null", "n/a", "unknown", "غير واضح"].includes(raw.toLowerCase())) return null;
  const iso = raw.match(/^(\d{4})[-\/](\d{1,2})[-\/](\d{1,2})/);
  if (iso) return `${iso[1]}-${iso[2].padStart(2, "0")}-${iso[3].padStart(2, "0")}`;
  const dmy = raw.match(/^(\d{1,2})[-\/](\d{1,2})[-\/](\d{4})/);
  if (dmy) return `${dmy[3]}-${dmy[2].padStart(2, "0")}-${dmy[1].padStart(2, "0")}`;
  return raw;
}
function parseReceiptJson(content) {
  const cleaned = content.replace(/<think>[\s\S]*?<\/think>/gi, "").trim();
  const jsonMatch = cleaned.match(/\{[\s\S]*\}/);
  if (!jsonMatch) return {
    amount: null,
    date: null
  };
  try {
    const parsed = JSON.parse(jsonMatch[0]);
    const amountRaw = parsed.amount;
    const amountText = amountRaw == null ? null : normalizeDigits(String(amountRaw));
    const amountNum = typeof amountRaw === "number" ? amountRaw : amountText != null ? Number(amountText.replace(/[^\d.]/g, "")) : null;
    const dateText = typeof parsed.date === "string" && parsed.date.trim() ? parsed.date.trim() : null;
    return {
      amount: amountNum != null && Number.isFinite(amountNum) ? amountNum : null,
      date: normalizeReceiptDate(dateText)
    };
  } catch {
    return {
      amount: null,
      date: null
    };
  }
}
async function fetchImageAsDataUrl(url) {
  if (url.startsWith("data:")) return url;
  const resp = await fetch(url);
  if (!resp.ok) throw new Error(`fetch image ${resp.status}`);
  const buf = await resp.arrayBuffer();
  const mime = resp.headers.get("content-type") ?? "image/jpeg";
  return `data:${mime};base64,${Buffer.from(buf).toString("base64")}`;
}
async function callGroqVision(model, imageDataUrl, apiKey, focused = false) {
  const res = await fetch("https://api.groq.com/openai/v1/chat/completions", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${apiKey}`
    },
    body: JSON.stringify({
      model,
      temperature: 0,
      max_completion_tokens: 300,
      response_format: {
        type: "json_object"
      },
      messages: [{
        role: "system",
        content: RECEIPT_VISION_PROMPT
      }, {
        role: "user",
        content: [{
          type: "text",
          text: focused ? RECEIPT_FOCUSED_PROMPT : "Read this receipt and extract amount and date as a JSON object."
        }, {
          type: "image_url",
          image_url: {
            url: imageDataUrl
          }
        }]
      }]
    })
  });
  if (!res.ok) {
    const txt = await res.text().catch(() => "");
    throw new Error(`Groq Vision ${model} ${res.status}: ${txt.slice(0, 200)}`);
  }
  const j = await res.json();
  return parseReceiptJson(j?.choices?.[0]?.message?.content ?? "");
}
async function readReceiptWithGroqVision(receiptUrl) {
  const apiKey = process.env.GROQ_API_KEY;
  if (!apiKey) throw new Error("GROQ_API_KEY غير موضع");
  const imageDataUrl = await fetchImageAsDataUrl(receiptUrl);
  let lastError = null;
  for (const model of VISION_MODELS) {
    try {
      let result = await callGroqVision(model, imageDataUrl, apiKey);
      if (result.amount === null || result.date === null) {
        const focusedResult = await callGroqVision(model, imageDataUrl, apiKey, true);
        result = {
          amount: result.amount ?? focusedResult.amount,
          date: result.date ?? focusedResult.date
        };
      }
      if (result.amount !== null || result.date !== null) {
        return {
          bankName: null,
          ...result
        };
      }
    } catch (e) {
      lastError = e instanceof Error ? e : new Error(String(e));
      console.error(`readReceipt: model ${model} failed`, e);
    }
  }
  if (lastError) throw lastError;
  return {
    bankName: null,
    amount: null,
    date: null
  };
}
async function checkReceipt(receiptFile, packageAmount) {
  let parsed;
  try {
    parsed = await readReceiptWithGroqVision(receiptFile);
  } catch (e) {
    console.error("verifyReceipt: فشل قراءة الإيصال", e);
    return {
      approved: false,
      reason: "تعذر قراءة الإيصال. حاول برفع صورة أوضح."
    };
  }
  const {
    amount,
    date
  } = parsed;
  if (!date) {
    return {
      approved: false,
      reason: "لم يتم العثور على تاريخ في الإيصال."
    };
  }
  const receiptDate = new Date(date);
  if (isNaN(receiptDate.getTime())) {
    return {
      approved: false,
      reason: "تاريخ الإيصال غير صالح."
    };
  }
  const hoursDiff = (Date.now() - receiptDate.getTime()) / 36e5;
  if (hoursDiff < 0 || hoursDiff > 72) {
    return {
      approved: false,
      reason: "تاريخ الإيصال خارج نطاق 72 ساعة المسموح."
    };
  }
  if (amount == null) {
    return {
      approved: false,
      reason: "لم يتم العثور على المبلغ في الإيصال."
    };
  }
  if (Math.abs(amount - packageAmount) > 0.01) {
    return {
      approved: false,
      reason: `المبلغ في الإيصال (${amount}) لا يطابق قيمة الباقة (${packageAmount}).`
    };
  }
  return {
    approved: true,
    reason: "تمت الموافقة على الإيصال."
  };
}
const verifyReceipt_createServerFn_handler = createServerRpc({
  id: "a846ccbb8e0324334f18c2344337c3d6370fd1557fd569e0d0f39a9e17f34183",
  name: "verifyReceipt",
  filename: "src/lib/support.functions.ts"
}, (opts) => verifyReceipt.__executeServer(opts));
const verifyReceipt = createServerFn({
  method: "POST"
}).inputValidator((d) => z.object({
  email: z.string().trim().min(1),
  receiptFile: z.string().trim().min(1),
  packageAmount: z.number().positive()
}).parse(d)).handler(verifyReceipt_createServerFn_handler, async ({
  data
}) => checkReceipt(data.receiptFile, data.packageAmount));
const createPackageTrialSubscription_createServerFn_handler = createServerRpc({
  id: "585f22b59b464df4b4843c564292572d6b66e16706f12ec871db320c1e45208f",
  name: "createPackageTrialSubscription",
  filename: "src/lib/support.functions.ts"
}, (opts) => createPackageTrialSubscription.__executeServer(opts));
const createPackageTrialSubscription = createServerFn({
  method: "POST"
}).middleware([requireAuth]).inputValidator((d) => z.object({
  email: z.string().trim().min(1),
  receiptFile: z.string().trim().min(1),
  packageAmount: z.number().positive(),
  durationMinutes: z.number().int().positive()
}).parse(d)).handler(createPackageTrialSubscription_createServerFn_handler, async ({
  data,
  context
}) => {
  assertAdmin(context.roles);
  const check = await checkReceipt(data.receiptFile, data.packageAmount);
  if (!check.approved) {
    return {
      ok: false,
      reason: check.reason
    };
  }
  const vipRepo = await import("./vip.repo-CycBrLVA.js");
  const row = await vipRepo.createTrialVip(data.email, data.durationMinutes);
  return {
    ok: true,
    id: row.id,
    email: row.email ?? data.email
  };
});
export {
  adminCloseChat_createServerFn_handler,
  adminCountOpenSupportChats_createServerFn_handler,
  adminDeleteAllSupport_createServerFn_handler,
  adminDeleteBotQa_createServerFn_handler,
  adminListBotQa_createServerFn_handler,
  adminListChatMessages_createServerFn_handler,
  adminListChats_createServerFn_handler,
  adminReplyChat_createServerFn_handler,
  adminUpsertBotQa_createServerFn_handler,
  createPackageTrialSubscription_createServerFn_handler,
  listBotQuestions_createServerFn_handler,
  startVisitorChat_createServerFn_handler,
  verifyReceipt_createServerFn_handler,
  visitorEndSession_createServerFn_handler,
  visitorEscalate_createServerFn_handler,
  visitorGetMessages_createServerFn_handler,
  visitorSendMessage_createServerFn_handler
};
